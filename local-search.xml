<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>剑指offer</title>
    <link href="/leetcode-lcr/"/>
    <url>/leetcode-lcr/</url>
    
    <content type="html"><![CDATA[<h2 id="除法"><a href="#除法" class="headerlink" title="除法"></a>除法</h2><p>不能使用除法的情况下实现除法的功能。</p><p>想法是这里依次的去减除数，知道不能减了为止。但是这样如果碰到被除数极大，除数极小，很可能会超时。一个思路是：尝试在不断倍增除数，扩大2 4 8 16 ……倍，让这样的分割越来越大，减的速度越来越快，是 logN 的时间复杂度。考虑到仍然可能会余数还要重复操作，所以一次减完还不够，有点类似TCP发包的慢启动环节。</p><blockquote><p><em>为了处理负数，把一开始的参数都强制用绝对值在后面的计算中。最后再判断正负</em></p></blockquote><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs c++"> <span class="hljs-keyword">while</span>(a &gt;= b)&#123;<br>   <span class="hljs-type">long</span> n = <span class="hljs-number">1</span>, base = b;<br>   <span class="hljs-keyword">while</span>((base &lt;&lt; <span class="hljs-number">1</span>) &lt;= a)&#123;<br>base &lt;&lt;= <span class="hljs-number">1</span>;<br>n &lt;&lt;= <span class="hljs-number">1</span>;<br>   &#125;<br><br>   a -= base;<br>   ans += n;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="二进制求值"><a href="#二进制求值" class="headerlink" title="二进制求值"></a>二进制求值</h2><p>给定两个 01 字符串 <code>a</code> 和 <code>b</code> ，请计算它们的和，并以二进制字符串的形式输出。<br>输入为 <strong>非空</strong> 字符串且只包含数字 <code>1</code> 和 <code>0</code>。</p><p>按照二进制加法求出加法后的当前位和进位，注意这里的求出方式：最后保存的答案不要通过数组到字符串的转换，直接就用string初始化。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">while</span>(i &gt;= <span class="hljs-number">0</span> || j &gt;= <span class="hljs-number">0</span> || carry)&#123;<br><span class="hljs-type">int</span> adda = (i &gt;= <span class="hljs-number">0</span>) ? a[i--]-<span class="hljs-string">&#x27;0&#x27;</span> : <span class="hljs-number">0</span>;<br><span class="hljs-type">int</span> addb = (j &gt;= <span class="hljs-number">0</span>) ? b[j--]-<span class="hljs-string">&#x27;0&#x27;</span> : <span class="hljs-number">0</span>;<br><br><span class="hljs-type">int</span> sum = adda + addb + carry;<br>ans.<span class="hljs-built_in">push_back</span>((<span class="hljs-type">char</span>)(sum % <span class="hljs-number">2</span> + <span class="hljs-string">&#x27;0&#x27;</span>));<br>carry = sum / <span class="hljs-number">2</span>;<br><br>&#125;<br><br><span class="hljs-built_in">reverse</span>(ans.<span class="hljs-built_in">begin</span>(), ans.<span class="hljs-built_in">end</span>());<br><span class="hljs-keyword">return</span> ans;<br></code></pre></td></tr></table></figure><h2 id="最大单词长度乘积"><a href="#最大单词长度乘积" class="headerlink" title="最大单词长度乘积"></a>最大单词长度乘积</h2><p>给定一个字符串数组 <code>words</code>，请计算当两个字符串 <code>words[i]</code> 和 <code>words[j]</code> 不包含相同字符时，它们长度的乘积的最大值。假设字符串中只包含英语的小写字母。如果没有不包含相同字符的一对字符串，返回 0。</p><p>两个字符串找乘积最大值也只能两两比较，加快的技巧在如何快速检查是否重叠。数组遍历时间爆炸，哈希表保存每一个字符串的 char 可行，但是必须更大的 set，不能每次比较单独生成，这样会很耗时。用数组记录每个字母是否出现过也是很好的办法，用到的还是位运算。</p><div class="note note-info">                        <div class="note-content">              <p>字符串保存处理——数组<br>用位运算把每一个字母位标记下来，字符串中出现字母为 1，否则为 0. 相对哈希表保存字符串更简单高效。  </p>            </div>          </div><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">char</span> ch: words[i])&#123;<br>mask[i] |= (<span class="hljs-number">1</span> &lt;&lt; ch - <span class="hljs-string">&#x27;a&#x27;</span>);<br>&#125;<br>&#125;<br><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j = i<span class="hljs-number">+1</span>; j &lt; n; j++)&#123;<br><span class="hljs-keyword">if</span>(!(mask[i] &amp; mask[j]))<br>maxlen = <span class="hljs-built_in">max</span>(maxlen, (<span class="hljs-type">int</span>)(words[i].<span class="hljs-built_in">length</span>() * words[j].<span class="hljs-built_in">length</span>()));<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>技术笔记</category>
      
      <category>算法题</category>
      
    </categories>
    
    
    <tags>
      
      <tag>算法</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>遇见百分百的女孩</title>
    <link href="/meet-hundred-percent-girl/"/>
    <url>/meet-hundred-percent-girl/</url>
    
    <content type="html"><![CDATA[<h1 id="四月一个晴朗的早晨，遇见一个百分之百的女孩"><a href="#四月一个晴朗的早晨，遇见一个百分之百的女孩" class="headerlink" title="四月一个晴朗的早晨，遇见一个百分之百的女孩"></a>四月一个晴朗的早晨，遇见一个百分之百的女孩</h1><p>四月一个晴朗的早晨，我在原宿后街同一个百分之百的女孩擦肩而过。<br>不讳地说，女孩算不得怎么漂亮，并无吸引人之处，衣着也不出众，脑后的头发执着地带有睡觉挤压的痕迹。年龄也已不小了—应该快有 30 了。严格地说来，恐怕很难称之为女孩。然而，相距 50 米开外我便一眼看出：对于我来说，她是个百分之百的女孩。从看见她身姿的那一瞬间，我的胸口便如发生地鸣一般的震颤，口中如沙漠干得沙沙作响。<br>或许你也有你的理想女孩。例如喜欢足颈细弱的女孩，眼睛大的女孩，十指绝对好看的女孩，或不明所以地迷上慢慢花时间进食的女孩。我当然有自己的偏爱。在饭店时就曾看邻桌一个女孩的鼻形看得发呆。</p><p>但要明确勾勒百分之百的女孩形象，任何人都无法做到。我就绝对想不起她长有怎样的鼻子。甚至是否有鼻子都已记不真切，现在我所能记的，只有她并非十分漂亮这一点。事情也真是不可思议。<br>“昨天在路上同一个百分之百的女孩擦肩而过。”我对一个人说。</p><p>“唔，”他应道，“人可漂亮？”</p><p>“不，不是说这个。”<br>“那，是合你口味那种类型喽？”</p><p>“记不得了。眼睛什么样啦，胸部是大是小啦，统统忘得一干二净。”</p><p>“莫名其妙啊！”</p><p>“是莫名其妙。”</p><p>“那么，”他显得兴味索然，“你做什么了？搭话了？还是跟踪了？”</p><p>“什么都没有做。”我说，“仅仅是擦肩而过。”</p><p>她由东往西走，我从西向东去，在四月里一个神清气爽的早晨。</p><p>我想和她说话，哪怕 30 分钟也好。想打听她的身世，也想全盘托出自己的身世。而更重要的，是想弄清导致 1981 年 4 月一个晴朗的早晨我们在原宿后街擦肩而过这一命运的原委。里面肯定充满和平时代的古老机器般温馨的秘密。</p><p>如此谈罢，我们可以找地方吃午饭，看伍迪。爱伦的影片，再顺路到宾馆里的酒吧喝鸡尾酒什么的。弄得好，喝完说不定能同她睡上一觉。</p><p>可能性在扣击我的心扉。</p><p>我和她之间的距离以近至十五六米了。</p><p>问题是，我到底该如何向她搭话呢？</p><p>“你好！和我说说话可以吗？哪怕 30 分钟也好。”</p><p>过于傻气，简直象劝人加入保险。</p><p>“请问，这一带有 24 小时营业的洗衣店吗？”</p><p>这也同样傻里傻气。何况我岂非连洗衣袋都没带！有谁能相信我的道白呢？</p><p>也许开门见山好些。“你好！你对我可是百分之百的女孩哟！”</p><p>不，不成，她恐怕不会相信我的表白。纵然相信，也未必愿同我说什么话。她可能这样说：即便我对你是百分之百的女孩，你对我可不是百分之百的男人，抱歉！而这是大有可能的。假如陷入这般境地，我肯定全然不知所措。这一打击说不定使我一蹶不振。我已 32 岁，所谓上年纪归根结底便是这么一回事。</p><p>我是在花店门前和她擦肩而过的，那暖暖的小小的气块儿触到我的肌肤。柏油路面洒了水，周围荡漾着玫瑰花香。连向她打声招呼我都未能做到。她身穿白毛衣，右手拿一个尚未贴邮票的四方信封。她给谁写了封信。那般睡眼惺忪，说不定整整写了一个晚上。那四方信封里有可能装有她的全部秘密。</p><p>走几步回头时，她的身影早已消失在人群中。</p><p>※ ※ ※<br>当然，今天我已完全清楚当时应怎样向她搭话。但不管怎么说，那道白实在太长，我笃定表达不好――就是这样，我所想到的每每不够实用。</p><p><strong>总之，道白自“很久很久以前”开始，而以“你不觉得这是个忧伤的故事吗”结束</strong>。</p><p>※ ※ ※<br>很久很久以前，有个地方有一个少男和一个少女。少男 18，少女 16。少男算不得英俊，少女也不怎么漂亮，无非随处可见的孤独而平常的少男少女。但两人一直坚信世上某个地方一定存在百分之百适合自己的少女和少男。是的，两人相信奇迹，而奇迹果真发生了。</p><p>一天两人在街头不期而遇。</p><p>“真巧！我一直在寻找你。也许你不相信，你对我是百分之百的男孩。从头到脚跟我想象的一模一样。简直是在做梦。”</p><p>两人坐在公园长椅上，手拉手，百谈不厌。两人已不再孤独。百分之百需求对方，百分之百已被对方需求。而百分之百需求对方和百分之百地被对方需求是何等美妙的事情啊！这已是宇宙奇迹！</p><p>但两人心中掠过一个小小的，的确小而又小的疑虑：梦想如此轻易成真是否就是好事？</p><p>交谈突然中断时，少男这样说道：</p><p>“我说，再尝试一次吧！如果我们两人真是一对百分之百的恋人的话，肯定还会有一天在哪里相遇。下次相遇时如果仍觉得对方百分之百，就马上在那里结婚，好么？</p><p>“好的。”少女回答。</p><p>于是两人分开，各奔东西。</p><p>然而说实在话，根本没有必要尝试，纯属多此一举。为什么呢？因为两人的的确确是一对百分之百的恋人，因为那是奇迹般的邂逅。但两人过于年轻，没办法知道这许多。于是无情的命运开始捉弄两人。</p><p>一年冬天，两人都染上了那年肆虐的恶性流感。在死亡线徘徊几个星期后，过去的记忆丧失殆尽。事情也真是离奇。当两人睁眼醒来时，脑袋里犹如 D。H 劳伦斯少年时代的贮币盒一样空空如也。</p><p>但这对青年男女毕竟聪颖豁达且极有毅力，经过不懈努力，终于再度获得了新的知识新的情感，胜任愉快地重返社会生活。啊，我的上帝！这两人真是无可挑剔！他们完全能够换乘地铁，能够在邮局寄交快信了。并且分别体验了百分之七十五和百分之八十五的恋爱。<br>如此一来二去，少男 32，少女 31 岁了。时光以惊人的速度流逝。</p><p>四月一个晴朗的早晨，少男为喝折价早咖啡沿原宿后街由西向东走，少女为买快信邮票沿同一条街由东向西去，两人恰在路中间失之交臂。失却的记忆的微光刹那间照亮两颗心。</p><p>两人胸口陡然悸颤，并且得知：</p><p>她对我是百分之百的女孩。</p><p>他对我是百分之百的男孩。<br>然而两人记忆的烛光委实过于微弱，两人的话语也不似十四年前那般清晰。结果连句话也没说便擦身而过，径直消失在人群中，永远永远。</p><p>你不觉得这是个令人感伤的故事么？</p><p><strong>是的，我本该这样向她搭话</strong>。</p>]]></content>
    
    
    <categories>
      
      <category>WLB</category>
      
      <category>摘录</category>
      
    </categories>
    
    
    <tags>
      
      <tag>书影音</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>算法题笔记-Hot100</title>
    <link href="/leetcode-hot100/"/>
    <url>/leetcode-hot100/</url>
    
    <content type="html"><![CDATA[<div class="note note-danger">                        <div class="note-content">              <p><em><strong>Remember, you are not a solution machine. You are a builder.</strong></em><br>笔记是个人思路的速记，不完整，详细的请直接参考题解。<br>自 2025-5 开始记录，之前做的题会在重做的时候更新，更完后此纪录会删除</p>            </div>          </div><h1 id="动态规划"><a href="#动态规划" class="headerlink" title="动态规划"></a>动态规划</h1><h2 id="分割等和子集"><a href="#分割等和子集" class="headerlink" title="分割等和子集"></a>分割等和子集</h2><p>给你一个  <strong>只包含正整数</strong>  的  <strong>非空</strong>  数组  <code>nums</code> 。请你判断是否可以将这个数组分割成两个子集，使得两个子集的元素和相等。</p><p>这道题是 01 背包问题的一个变种，问题可以看成在整个数组当中能不能挑出一些数使得这个数刚好是总和的一半。这里就有两个限制条件首先是数组的总和必须是偶数才能够被等分，另一个是数组当中的最大元素不能超过总和的一半。用<code>dp[i][j]</code>表示数组前 i 个数能不能选出一些数使得总和等于 j，我们需要构造出这个二维数组，它的大小是：（数组元素的个数和速度总和的一半+1）<br>接下来是初始化，</p><ul><li>所有元素都能够构造出一颗总和为零的值，只要不选就可以.</li><li><code>dp[0]</code>只能构造出 0 和<code>nums[0]</code>这两个总和<br>构造这个二维数组，状态转移是遍历数组当中的数，考虑是不是把它加进背包当中。如果这个数已经大于了 j，那么直接不选；如果小于 J 那么可选可不选。这里要好好理解 <code>dp[i][j]</code> 的含义：能在前 i 个数凑出总和为 j 的情况只能是 <code>dp[i-1][j-nums[i]] == true</code> 刚好加上<code>nums[i]</code>；或者不选这个数，在此之前就能凑够。</li></ul><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt;= target; j++)&#123;<br><span class="hljs-keyword">if</span>(nums[i] &gt; j)<br>dp[i][j] = dp[i<span class="hljs-number">-1</span>][j];<br><span class="hljs-keyword">else</span>&#123;<br>dp[i][j] = dp[i<span class="hljs-number">-1</span>][j-nums[i]] || dp[i<span class="hljs-number">-1</span>][j];  <span class="hljs-comment">// 这里用到的就是dp的核心，能不能凑出总和j，在于选不选nums[i]</span><br>&#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="最长回文子串"><a href="#最长回文子串" class="headerlink" title="最长回文子串"></a>最长回文子串</h2><p>给你一个字符串 <code>s</code>，找到 <code>s</code> 中最长的 回文 子串。</p><p>回文串特性是判断回文要依赖子串，这道题先遍历子串的长度从 1…..n，这样利用之前得到的子串长度就能判断了，不需要倒着遍历。不过这样子时间复杂度有点高是$O(n^2)$ ,也有一些技巧比如说判断首尾字符相等的时候如果字串长度&lt;3 那么直接判断为回文串。只有在判断回文串的长度能不能扩展的时候用到了动态规划的思想，这道题重点更在于对一个长串进行两层子串的遍历。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> L = <span class="hljs-number">2</span>; L &lt;= len; L++)&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; len; i++)&#123;<br><span class="hljs-type">int</span> j = i + L <span class="hljs-number">-1</span>;<br><br><span class="hljs-keyword">if</span>(j &gt;= len)<br><span class="hljs-keyword">break</span>;<br><span class="hljs-keyword">if</span>(s[i] == s[j])&#123;<br><span class="hljs-keyword">if</span>(j - i &lt; <span class="hljs-number">3</span>)   dp[i][j] = <span class="hljs-literal">true</span>;<br><span class="hljs-keyword">else</span>    dp[i][j] = dp[i<span class="hljs-number">+1</span>][j<span class="hljs-number">-1</span>];  <span class="hljs-comment">// 子串可以扩展</span><br>&#125;<span class="hljs-keyword">else</span>&#123;<br>dp[i][j] = <span class="hljs-literal">false</span>;<br>&#125;<br><br><span class="hljs-keyword">if</span>(dp[i][j])&#123;<br>maxlen = <span class="hljs-built_in">max</span>(maxlen, j-i<span class="hljs-number">+1</span>);<br>begin = i;<br>&#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="最长公共子序列"><a href="#最长公共子序列" class="headerlink" title="最长公共子序列"></a>最长公共子序列</h2><p>给定两个字符串 <code>text1</code> 和 <code>text2</code>，返回这两个字符串的最长 <strong>公共子序列</strong> 的长度。如果不存在 <strong>公共子序列</strong> ，返回 <code>0</code> 。<br>一个字符串的 <strong>子序列</strong> 是指这样一个新的字符串：它是由原字符串在不改变字符的相对顺序的情况下删除某些字符（也可以不删除任何字符）后组成的新字符串。</p><p>初始化的时候要注意不能从 0 开始，要么同时初始化 00 01 10 不然就用偏移数组的方法.因为会用到<br><code>dp[i][j] = max(dp[i-1][j], dp[i][j-1]);</code></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= n; i++)&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j = <span class="hljs-number">1</span>; j &lt;= m; j++)&#123;<br><span class="hljs-keyword">if</span>(text1[i<span class="hljs-number">-1</span>] == text2[j<span class="hljs-number">-1</span>])&#123;  <span class="hljs-comment">// 偏移了，相当于把(i-1, j-1)记录在dp[i, j], 最后返回的时候是dp[n][m]</span><br>dp[i][j] = dp[i<span class="hljs-number">-1</span>][j<span class="hljs-number">-1</span>] + <span class="hljs-number">1</span>;<br><span class="hljs-comment">// lcs = max(lcs, dp[i][j]);</span><br>&#125;<span class="hljs-keyword">else</span>&#123;<br>dp[i][j] = <span class="hljs-built_in">max</span>(dp[i<span class="hljs-number">-1</span>][j], dp[i][j<span class="hljs-number">-1</span>]);<br>&#125;<br>lcs = <span class="hljs-built_in">max</span>(lcs, dp[i][j]);<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> lcs;<br></code></pre></td></tr></table></figure><h2 id="编辑距离"><a href="#编辑距离" class="headerlink" title="编辑距离"></a>编辑距离</h2><p>给你两个单词 <code>word1</code> 和 <code>word2</code>， <em>请返回将 <code>word1</code> 转换成 <code>word2</code> 所使用的最少操作数</em>  。<br>你可以对一个单词进行如下三种操作：</p><ul><li>插入一个字符</li><li>删除一个字符</li><li>替换一个字符</li></ul><p>理解 word1 怎么进行操作会变成 word2，还是有最优子结构的。插入考虑的是<code>(i, j-1 )</code>的情况下，word1 再插入<code>word2[j]</code> ；删除是 word1 多了，考虑<code>(i-1, j)+1</code>, 替换是都后退</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= n;i++)&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j = <span class="hljs-number">1</span>; j &lt;= m; j++)&#123;<br><span class="hljs-keyword">if</span>(word1[i<span class="hljs-number">-1</span>] == word2[j<span class="hljs-number">-1</span>])<br>dp[i][j] = dp[i<span class="hljs-number">-1</span>][j<span class="hljs-number">-1</span>];<br><span class="hljs-keyword">else</span>&#123;<br>dp[i][j] = <span class="hljs-built_in">min</span>(dp[i<span class="hljs-number">-1</span>][j]<span class="hljs-number">+1</span>, <span class="hljs-built_in">min</span>(dp[i<span class="hljs-number">-1</span>][j<span class="hljs-number">-1</span>]<span class="hljs-number">+1</span>, dp[i][j<span class="hljs-number">-1</span>]<span class="hljs-number">+1</span>));<br>&#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h1><h2 id="LRU-cache"><a href="#LRU-cache" class="headerlink" title="LRU cache"></a>LRU cache</h2><p>请你设计并实现一个满足 LRU (最近最少使用) 缓存 约束的数据结构。<br>实现 <code>LRUCache</code> 类：</p><ul><li><code>LRUCache(int capacity)</code> 以 <strong>正整数</strong> 作为容量 <code>capacity</code> 初始化 LRU 缓存</li><li><code>int get(int key)</code> 如果关键字 <code>key</code> 存在于缓存中，则返回关键字的值，否则返回 <code>-1</code> 。</li><li><code>void put(int key, int value)</code> 如果关键字 <code>key</code> 已经存在，则变更其数据值 <code>value</code> ；如果不存在，则向缓存中插入该组 <code>key-value</code> 。如果插入操作导致关键字数量超过 <code>capacity</code> ，则应该 <strong>逐出</strong> 最久未使用的关键字。<br>函数 <code>get</code> 和 <code>put</code> 必须以 <code>O(1)</code> 的平均时间复杂度运行。</li></ul><p>这道题的思路是要求在 O1 的时间内实现插入和更新，如果只使用链表的话那么遍历整个链表，寻找到需要更新的需要 $O(n)$ 时间复杂度。考虑只使用链表的话同时还需要维护每一次被访问后的更新值，用来在内存满的时候删除最久未使用节点。如果只使用哈希表，怎么没有办法维护LRU满的情况。</p><p>实现的方式是：哈希表实现 get 的 O1 定位，每一次的访问后把节点提前到链表首部，如果满了就删除链表末尾的节点（最久未使用&#x2F;访问）。<br>注意这里只有在 cache 满的时候才会删除节点 <code>delete</code> ，更新节点并不需要删除节点，只需要移动，这里就是双向链表的好处。注意，这里 head，tail 都是固定的节点，不包含真正的数据。map 记录了key -&gt; node* 关系，如果要删除节点需要删除哈希表项。每一次的更新都是删去老节点，增加新结点，避免了在链表当中移动节点。在这个过程当中哈希表项也是变化的，因为记录的节点指针不同。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-type">int</span> key)</span> </span>&#123;<br><span class="hljs-keyword">if</span>(map.<span class="hljs-built_in">find</span>(key) == map.<span class="hljs-built_in">end</span>())<br><span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br><br>Node* iter = map[key];<br><span class="hljs-comment">// 已经更新了键值对，不能直接返回</span><br><span class="hljs-built_in">remove</span>(iter);<br><span class="hljs-built_in">add</span>(iter);<br><span class="hljs-keyword">return</span> iter-&gt;val;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">put</span><span class="hljs-params">(<span class="hljs-type">int</span> key, <span class="hljs-type">int</span> value)</span> </span>&#123;<br><span class="hljs-keyword">if</span>(map.<span class="hljs-built_in">find</span>(key) == map.<span class="hljs-built_in">end</span>())&#123;<br>Node* p = <span class="hljs-keyword">new</span> <span class="hljs-built_in">Node</span>(key, value);<br><span class="hljs-keyword">if</span>(map.<span class="hljs-built_in">size</span>() == cap)&#123;<br>Node* last = tail-&gt;pre;<br><span class="hljs-built_in">removeLast</span>();<br>map.<span class="hljs-built_in">erase</span>(last-&gt;key);<br><span class="hljs-built_in">delete</span>(last);<br>&#125;<br><span class="hljs-built_in">add</span>(p);<br>map[key] = p;<br>&#125;<span class="hljs-keyword">else</span>&#123;<br>Node* p = map[key];<br>p-&gt;val = value;<br><span class="hljs-built_in">remove</span>(p);<br><span class="hljs-built_in">add</span>(p);<br>&#125;<br>&#125;<br>.....<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">remove</span><span class="hljs-params">(Node* node)</span></span>&#123;<br>node-&gt;pre-&gt;next = node-&gt;next;<br>node-&gt;next-&gt;pre = node-&gt;pre;<br>&#125;<br><br><span class="hljs-comment">// 新节点放在最前面</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">add</span><span class="hljs-params">(Node* node)</span></span>&#123;<br>node-&gt;next = head-&gt;next;<br>head-&gt;next-&gt;pre = node;<br>head-&gt;next = node;<br>node-&gt;pre = head;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">removeLast</span><span class="hljs-params">()</span></span>&#123;<br>tail-&gt;pre-&gt;pre-&gt;next = tail;<br>tail-&gt;pre = tail-&gt;pre-&gt;pre;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="K-个一组反转链表"><a href="#K-个一组反转链表" class="headerlink" title="K 个一组反转链表"></a>K 个一组反转链表</h2><p>创建一个虚节点对每 k 各一组的节点进行翻转，还需要连接下一组的起始节点，HARD</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">while</span>(<span class="hljs-literal">true</span>)&#123;<br>ListNode* tail = pre;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; k; i++)&#123;<br>tail = tail-&gt;next;<br><span class="hljs-keyword">if</span>(tail == <span class="hljs-literal">nullptr</span>)<br><span class="hljs-keyword">return</span> dummy-&gt;next;<br>&#125;<br><br>ListNode *nextGroup = tail-&gt;next;<br>ListNode* index = pre-&gt;next, *prev = nextGroup;<br><br><span class="hljs-keyword">while</span>(index != nextGroup)&#123;<br>ListNode* tmp = index-&gt;next;<br>index-&gt;next = prev;<br>prev = index;<br>index = tmp;<br>&#125;<br><br>ListNode* oldhead = pre-&gt;next;<br>pre-&gt;next = tail;<br>pre = oldhead;   <span class="hljs-comment">// 现在oldhead是反转过后的尾节点，正好是下一组要反转的前一个节点</span><br>&#125;<br></code></pre></td></tr></table></figure><h2 id="随机链表的复制"><a href="#随机链表的复制" class="headerlink" title="随机链表的复制"></a>随机链表的复制</h2><p>给你一个长度为 <code>n</code> 的链表，每个节点包含一个额外增加的随机指针 <code>random</code> ，该指针可以指向链表中的任何节点或空节点。<br>构造这个链表的 <strong><a href="https://baike.baidu.com/item/%E6%B7%B1%E6%8B%B7%E8%B4%9D/22785317?fr=aladdin">深拷贝</a></strong>。 深拷贝应该正好由 <code>n</code> 个 <strong>全新</strong> 节点组成，其中每个新节点的值都设为其对应的原节点的值。新节点的 <code>next</code> 指针和 <code>random</code> 指针也都应指向复制链表中的新节点，并使原链表和复制链表中的这些指针能够表示相同的链表状态。<strong>复制链表中的指针都不应指向原链表中的节点</strong> 。</p><p>todo</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs c++">Node *new_head = <span class="hljs-keyword">new</span> <span class="hljs-built_in">Node</span>(head-&gt;val), *p = head, *q = new_head;<br><span class="hljs-type">int</span> index = <span class="hljs-number">0</span>;<br><br><span class="hljs-keyword">while</span>(p)&#123;<br><span class="hljs-keyword">if</span>(p-&gt;next)&#123;<br>q-&gt;next = <span class="hljs-keyword">new</span> <span class="hljs-built_in">Node</span>(p-&gt;next-&gt;val);<br>listmap[p] = index;<br>newmap[index] = q;<br>index++;<br>p = p-&gt;next;<br>q = q-&gt;next;<br>&#125;<br><span class="hljs-keyword">else</span>&#123;<br>listmap[p] = index;<br>newmap[index] = q;<br>q-&gt;next = <span class="hljs-literal">nullptr</span>;<br>p = p-&gt;next;<br>q = q-&gt;next;<br>&#125;<br>&#125;<br><br><span class="hljs-keyword">for</span>(q = new_head, p = head; p || q; p = p-&gt;next, q = q-&gt;next)&#123;<br><span class="hljs-keyword">if</span>(p-&gt;random == <span class="hljs-literal">nullptr</span>) &#123; q-&gt;random = <span class="hljs-literal">nullptr</span>; <span class="hljs-keyword">continue</span>;&#125;<br><br><span class="hljs-type">int</span> random_index = listmap[p-&gt;random];<br>q-&gt;random = newmap[random_index];<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="合并-k-个升序链表"><a href="#合并-k-个升序链表" class="headerlink" title="合并 k 个升序链表"></a>合并 k 个升序链表</h2><p>给你一个链表数组，每个链表都已经按升序排列。<br>请你将所有链表合并到一个升序链表中，返回合并后的链表。</p><p>所有链表的表头都放到最小堆里，每次产生一个最小值加到新链表的末尾，如果判断使用的节点还有 next 节点下一个节点也加入到堆当中。注意维护如果一个链表为空，则不继续添加。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">while</span>(!heap.<span class="hljs-built_in">empty</span>())&#123;<br>ListNode* curr = heap.<span class="hljs-built_in">top</span>();<br>index-&gt;next = curr;<br>index = index-&gt;next;<br><br>heap.<span class="hljs-built_in">pop</span>();<br><span class="hljs-keyword">if</span>(curr-&gt;next)  <span class="hljs-comment">// 后面还有节点</span><br>heap.<span class="hljs-built_in">push</span>(curr-&gt;next);<br><br>&#125;<br></code></pre></td></tr></table></figure><h1 id="双指针"><a href="#双指针" class="headerlink" title="双指针"></a>双指针</h1><h2 id="三数之和"><a href="#三数之和" class="headerlink" title="三数之和"></a>三数之和</h2><p>给你一个整数数组 <code>nums</code> ，判断是否存在三元组 <code>[nums[i], nums[j], nums[k]]</code> 满足 <code>i != j</code>、<code>i != k</code> 且 <code>j != k</code> ，同时还满足 <code>nums[i] + nums[j] + nums[k] == 0</code> 。请你返回所有和为 <code>0</code> 且不重复的三元组。<br><strong>注意</strong>：答案中不可以包含重复的三元组。</p><p>两数之和的解法是用哈希表，边遍历边查表几乎只需要 $O(n^2)$ 时间复杂度即可。哈希表相当于一层快速查询，那么三数之和就需要固定两个游标，这样的确可以快速找到一个解，但是不能继续移动维护游标。考虑去重的话，也不好操作。所以三数 四叔都是用双指针写法，排序后固定一个数，之后进行剪枝和双指针查询。</p><p>具体剪枝是在找到一个三数之和等于0下，跳过了左右指针相同的数字；第一个固定指针相同数字也被跳过。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-keyword">if</span>(i &amp;&amp; nums[i] == nums[i<span class="hljs-number">-1</span>])<br><span class="hljs-keyword">continue</span>;<br><br><span class="hljs-type">int</span> l = i<span class="hljs-number">+1</span>, r = nums.<span class="hljs-built_in">size</span>()<span class="hljs-number">-1</span>;<br><span class="hljs-keyword">while</span>(l &lt; r)&#123;<br><span class="hljs-type">int</span> sum = nums[i] + nums[l] + nums[r];<br><span class="hljs-keyword">if</span>(!sum)&#123;<br>ans.<span class="hljs-built_in">push_back</span>(&#123;nums[i], nums[l], nums[r]&#125;);<br><span class="hljs-keyword">while</span>(l &lt; r &amp;&amp; nums[l] == nums[l<span class="hljs-number">+1</span>]) l++;<br><span class="hljs-keyword">while</span>(l &lt; r &amp;&amp; nums[r] == nums[r<span class="hljs-number">-1</span>]) r--;<br>l++, r--;<br>&#125;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(sum &lt; <span class="hljs-number">0</span>)    l++;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(sum &gt; <span class="hljs-number">0</span>)    r--;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="接雨水"><a href="#接雨水" class="headerlink" title="接雨水"></a>接雨水</h2><p>给定 <code>n</code> 个非负整数表示每个宽度为 <code>1</code> 的柱子的高度图，计算按此排列的柱子，下雨之后能接多少雨水。</p><h1 id="滑动窗口"><a href="#滑动窗口" class="headerlink" title="滑动窗口"></a>滑动窗口</h1><h2 id="寻找异位词"><a href="#寻找异位词" class="headerlink" title="寻找异位词"></a>寻找异位词</h2><p>给定两个字符串 <code>s</code> 和 <code>p</code>，找到 <code>s</code> 中所有 <code>p</code> 的 <strong>异位词</strong> 的子串，返回这些子串的起始索引。不考虑答案输出的顺序。</p><p>固定目标串长度的窗口，并且每个窗口都是用字母计数桶标记的，依次和匹配串比较，如果两个桶相同，那么就找到了</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; s.<span class="hljs-built_in">length</span>() - p.<span class="hljs-built_in">length</span>(); i++)&#123;<br>window[s[i] - <span class="hljs-string">&#x27;a&#x27;</span>]--;<br>window[s[i+p.<span class="hljs-built_in">length</span>()] - <span class="hljs-string">&#x27;a&#x27;</span>]++;<br><br><span class="hljs-keyword">if</span>(arrp == window)<br>ans.<span class="hljs-built_in">push_back</span>(i<span class="hljs-number">+1</span>);<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="无重复字符的最长子串"><a href="#无重复字符的最长子串" class="headerlink" title="无重复字符的最长子串"></a>无重复字符的最长子串</h2><p>给定一个字符串 <code>s</code> ，请你找出其中不含有重复字符的 <strong>最长 子串</strong> 的长度。</p><p>窗口内记录无重复字符的子串，是用哈希表判断右指针遇到的字符是不是已经出现过了。如果没有出现，右边界继续移动；否则找到哈希表记录的重复字符索引，左边界跳转。在这个过程中更新最大窗口长度。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">while</span>(r &lt; s.<span class="hljs-built_in">length</span>())&#123;<br><span class="hljs-keyword">if</span>(map.<span class="hljs-built_in">find</span>(s[r]) != map.<span class="hljs-built_in">end</span>() &amp;&amp; map[s[r]] &gt;= l)&#123;  <span class="hljs-comment">// 保证L只会向前移动</span><br>l = map[s[r]] + <span class="hljs-number">1</span>;<br>&#125;<br>map[s[r]] = r;<br>maxw = <span class="hljs-built_in">max</span>(maxw, r-l<span class="hljs-number">+1</span>);<br>r++;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="栈"><a href="#栈" class="headerlink" title="栈"></a>栈</h1><h2 id="柱状图中的最大矩形"><a href="#柱状图中的最大矩形" class="headerlink" title="柱状图中的最大矩形"></a>柱状图中的最大矩形</h2><p>给定 <em>n</em> 个非负整数，用来表示柱状图中各个柱子的高度。每个柱子彼此相邻，且宽度为 1 。<br>求在该柱状图中，能够勾勒出来的矩形的最大面积。<br><strong>输入：</strong> heights &#x3D; <code>[2,1,5,6,2,3]</code><br><strong>输出：</strong> 10</p><p>限制这道题面积的是长和宽，我们尽量找更高的矩形，尽可能的拓宽宽度边界。这道题的思路是：找到一个区间确保能快速找到左右的比他低的索引，保证除了这两个边界，这个范围内最低的高度就是它本身，即高度。相当于一个 “凹” 字形，计算出来的矩形面级物理意义是：<strong>当前柱子高度作为某个区间内的最矮，以此计算可能的最大面积</strong>。保证了当前柱子这是用单调栈做到的，<strong>和下面的接雨水题目很类似</strong>，思想是共通的。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; ++i) &#123;<br><span class="hljs-comment">// 当前柱子高度 &lt; 栈顶柱子高度，说明可以结算了，不满足单调特性</span><br><span class="hljs-keyword">while</span> (!st.<span class="hljs-built_in">empty</span>() &amp;&amp; heights[i] &lt; heights[st.<span class="hljs-built_in">top</span>()]) &#123;<br><span class="hljs-type">int</span> height = heights[st.<span class="hljs-built_in">top</span>()];  <span class="hljs-comment">// 当前出栈柱子的高度</span><br>st.<span class="hljs-built_in">pop</span>();<br><br><span class="hljs-type">int</span> right = i;<br><span class="hljs-type">int</span> left = st.<span class="hljs-built_in">top</span>();  <span class="hljs-comment">// 出栈后的栈顶是“左边第一个比它矮的”</span><br><span class="hljs-type">int</span> width = right - left - <span class="hljs-number">1</span>;  <span class="hljs-comment">// 注意这里要减去两个边界，非常迷惑，其实是一个“凹”区间</span><br>maxArea = <span class="hljs-built_in">max</span>(maxArea, height * width);  <span class="hljs-comment">// 最低高度</span><br>&#125;<br>st.<span class="hljs-built_in">push</span>(i);  <span class="hljs-comment">// 当前柱子入栈</span><br>&#125;<br></code></pre></td></tr></table></figure><h2 id="接雨水-1"><a href="#接雨水-1" class="headerlink" title="接雨水"></a>接雨水</h2><p>单调栈的做法，构成一个单调减的栈，出现高的柱子代表出现了一个洼地，可以计算这一部分的雨水。雨水的计算是分割成一个又一个小块的，最终堆出来。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; height.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-keyword">while</span>(!st.<span class="hljs-built_in">empty</span>() &amp;&amp; height[i] &gt; height[st.<span class="hljs-built_in">top</span>()])&#123;<br><span class="hljs-type">int</span> bottom = st.<span class="hljs-built_in">top</span>();<br>st.<span class="hljs-built_in">pop</span>();<br><span class="hljs-keyword">if</span>(st.<span class="hljs-built_in">empty</span>())<br><span class="hljs-keyword">break</span>;<br><span class="hljs-type">int</span> left = st.<span class="hljs-built_in">top</span>();<br>ans += (<span class="hljs-built_in">min</span>(height[i], height[left]) - height[bottom]) * (i - left - <span class="hljs-number">1</span>);<br>&#125;<br>st.<span class="hljs-built_in">push</span>(i);<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="每日温度"><a href="#每日温度" class="headerlink" title="每日温度"></a>每日温度</h2><p>给定一个整数数组 <code>temperatures</code> ，表示每天的温度，返回一个数组 <code>answer</code> ，其中 <code>answer[i]</code> 是指对于第 <code>i</code> 天，下一个更高温度出现在几天后。如果气温在这之后都不会升高，请在该位置用 <code>0</code> 来代替。</p><p>单调栈的典型题目。维护一个单调递减的温度序列，如果下一个温度违反了单调性，那么依次将栈中的温度 pop，计算索引之间的差（即下一个更高的温度天数）。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; temperatures.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-keyword">if</span>(tem.<span class="hljs-built_in">empty</span>())<br>tem.<span class="hljs-built_in">push</span>(<span class="hljs-built_in">make_pair</span>(i, temperatures[i]));<br><span class="hljs-keyword">else</span>&#123;<br><span class="hljs-keyword">while</span>(!tem.<span class="hljs-built_in">empty</span>() &amp;&amp; tem.<span class="hljs-built_in">top</span>().second &lt; temperatures[i])&#123;<br>ans[tem.<span class="hljs-built_in">top</span>().first] = i - tem.<span class="hljs-built_in">top</span>().first;<br>tem.<span class="hljs-built_in">pop</span>();<br>&#125;<br>tem.<span class="hljs-built_in">push</span>(<span class="hljs-built_in">make_pair</span>(i, temperatures[i]));<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="字符串解码"><a href="#字符串解码" class="headerlink" title="字符串解码"></a>字符串解码</h2><p>给定一个经过编码的字符串，返回它解码后的字符串。<br>编码规则为: <code>k[encoded_string]</code>，表示其中方括号内部的 <code>encoded_string</code> 正好重复 <code>k</code> 次。注意 <code>k</code> 保证为正整数。<br>例如：s &#x3D; <code>&quot;3[a]2[bc]&quot;</code> 输出：<code>&quot;aaabcbc&quot;</code></p><p>解题的思路是一直记忆最长的匹配到的字符串 currentString，如果出现了 <code>[</code>，就表示 currentString 需要清空给 <code>[</code> 里面的字符串重复操作。用 numstack 维护需要重复的次数，<code>]</code> 遇到之后进行结算。同时测试样例还给了 <code>[]</code>嵌套出现的例子，我们临时的string需要保存在栈中，这样不会进入下一个 <code>[</code>的时候丢失前面的string导致连不上。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-keyword">auto</span> ch : s)&#123;<br><span class="hljs-keyword">if</span>(ch &gt;= <span class="hljs-string">&#x27;0&#x27;</span> &amp;&amp; ch &lt;= <span class="hljs-string">&#x27;9&#x27;</span>)&#123;<br>n = n*<span class="hljs-number">10</span> + ch - <span class="hljs-string">&#x27;0&#x27;</span>;<br>&#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (ch &gt;= <span class="hljs-string">&#x27;a&#x27;</span> &amp;&amp; ch &lt;= <span class="hljs-string">&#x27;z&#x27;</span>)&#123;<br>currentString += ch;<br>&#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(ch == <span class="hljs-string">&#x27;[&#x27;</span>)&#123;<br>numStack.<span class="hljs-built_in">push</span>(n);  <span class="hljs-comment">// []内部需要循环多少次，在这里保存</span><br>stringStack.<span class="hljs-built_in">push</span>(currentString);  <span class="hljs-comment">// []可能是嵌套递归出现的，所以暂时的string都需要保存下来</span><br>currentString.<span class="hljs-built_in">clear</span>();<br>n = <span class="hljs-number">0</span>;<br>&#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(ch == <span class="hljs-string">&#x27;]&#x27;</span>)&#123;<br><span class="hljs-type">int</span> times = numStack.<span class="hljs-built_in">top</span>();<br>string loop = <span class="hljs-string">&quot;&quot;</span>;<br><span class="hljs-keyword">while</span>(times--)&#123;<br>loop += currentString;<br>&#125;<br>currentString = stringStack.<span class="hljs-built_in">top</span>() + loop;<br>stringStack.<span class="hljs-built_in">pop</span>();<br>numStack.<span class="hljs-built_in">pop</span>();<br>&#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><h1 id="堆"><a href="#堆" class="headerlink" title="堆"></a>堆</h1><h2 id="寻找数据流的中位数"><a href="#寻找数据流的中位数" class="headerlink" title="寻找数据流的中位数"></a>寻找数据流的中位数</h2><p>实现 MedianFinder 类:</p><ul><li><code>MedianFinder()</code> 初始化 <code>MedianFinder</code> 对象。</li><li><code>void addNum(int num)</code> 将数据流中的整数 <code>num</code> 添加到数据结构中。</li><li><code>double findMedian()</code> 返回到目前为止所有元素的中位数。与实际答案相差 <code>10-5</code> 以内的答案将被接受。</li></ul><p>维护一个最小堆和最大堆，这里理解上相反。小于中位数的位于最大堆，top 是最接近中位数的；同样，大于中位数的是最小堆。每次先把数加到最大堆，然后给最小堆 top。如果不满足两个堆个数相差 1，则进行调整。这里就是维护中位数了。具体来说是哪一步保证了两个堆的堆顶就是中位数区间呢？是判断大顶堆和小顶堆个数相差 1，并且大顶堆进行了内部的调整（包括小顶堆的内部调整）共同来决定的。</p><blockquote><p><em>至于为什么是相差 1，是为了正好平分找到中位数，不过不能直接修改相差值找中位数后的第 N 大数</em><br>如果要找到第 K 大的数，可以维护一个大小为 K 的最小堆，不断遍历加入所有的数，最后堆顶就是第 K 大的数</p></blockquote><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs c++">priority_queue&lt;<span class="hljs-type">int</span>, vector&lt;<span class="hljs-type">int</span>&gt;, less&lt;<span class="hljs-type">int</span>&gt;&gt; minHeap;  <span class="hljs-comment">// 初始化大顶堆的比较函数是less&lt;int&gt;</span><br>priority_queue&lt;<span class="hljs-type">int</span>, vector&lt;<span class="hljs-type">int</span>&gt;, greater&lt;<span class="hljs-type">int</span>&gt;&gt; maxHeap;<br>……<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">addNum</span><span class="hljs-params">(<span class="hljs-type">int</span> num)</span> </span>&#123;<br><span class="hljs-keyword">if</span>(minHeap.<span class="hljs-built_in">size</span>() == <span class="hljs-number">0</span> || num &lt; minHeap.<span class="hljs-built_in">top</span>())&#123;<br>minHeap.<span class="hljs-built_in">push</span>(num);<br><span class="hljs-keyword">if</span>(maxHeap.<span class="hljs-built_in">size</span>() + <span class="hljs-number">1</span> &lt; minHeap.<span class="hljs-built_in">size</span>())&#123;<br>maxHeap.<span class="hljs-built_in">push</span>(minHeap.<span class="hljs-built_in">top</span>());<br>minHeap.<span class="hljs-built_in">pop</span>();<br>&#125;<br>&#125;<span class="hljs-keyword">else</span>&#123;<br>maxHeap.<span class="hljs-built_in">push</span>(num);<br><span class="hljs-keyword">if</span>(maxHeap.<span class="hljs-built_in">size</span>() &gt; minHeap.<span class="hljs-built_in">size</span>())&#123;<br>minHeap.<span class="hljs-built_in">push</span>(maxHeap.<span class="hljs-built_in">top</span>());<br>maxHeap.<span class="hljs-built_in">pop</span>();<br>&#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h1><h2 id="最小缺失的正数"><a href="#最小缺失的正数" class="headerlink" title="最小缺失的正数"></a>最小缺失的正数</h2><p>数组重新置位的方法感觉比较有趣，对每一个 <code>nums[i]</code> 都去正确的位置 <code>nums[nums[i] - 1]</code> ，这里暗示了能够处理的只有 val 属于(1, nums.size)范围内，如果超出了范围，保持不动。假设出现了这么一个数，前一个+1 就是答案。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-keyword">while</span>(nums[i] &gt; <span class="hljs-number">0</span> &amp;&amp; nums[i] &lt; nums.<span class="hljs-built_in">size</span>() &amp;&amp; nums[i] != nums[nums[i]<span class="hljs-number">-1</span>])&#123;<br><span class="hljs-built_in">swap</span>(nums[i], nums[nums[i]<span class="hljs-number">-1</span>]);<br>&#125;<br>&#125;<br><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-keyword">if</span>(nums[i] != i<span class="hljs-number">+1</span>)&#123;<br><span class="hljs-keyword">return</span> i == <span class="hljs-number">0</span> ? <span class="hljs-number">1</span> : nums[i<span class="hljs-number">-1</span>]<span class="hljs-number">+1</span>;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="子串"><a href="#子串" class="headerlink" title="子串"></a>子串</h1><blockquote><p>[!tip]<br>滑动窗口是子串中经常出现的，核心是找到移动左右边界的情况，在循环中不断移动边界，在所有的极值中找到最值。判断什么情况要移动边界是有针对题目的技巧，下面有题目思考。</p></blockquote><h2 id="滑动窗口最大值"><a href="#滑动窗口最大值" class="headerlink" title="滑动窗口最大值"></a>滑动窗口最大值</h2><p>维护一个单调递减的双端队列，这个队列向右滑动的时候，判断如果比队列当中最后一个数还要大那么就循环弹出队列尾部，这里面的道理是：因为新进来的数要比原窗口当中的数要大，并且它的位置更靠右，所以后面要找窗口内的最大值一定找更”新“的，并且它的值还更大，那么就肯定不会用到那些被弹出的数。那么窗口什么时候缩小呢？检查 <em>队列队首元素它的下标如果不满足窗口的长度</em>，那么就可以把它弹出去了，因为它已经不在窗口里了。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-keyword">while</span>(!q.<span class="hljs-built_in">empty</span>() &amp;&amp; nums[q.<span class="hljs-built_in">back</span>()] &lt; nums[i])&#123;<br>q.<span class="hljs-built_in">pop_back</span>();<br>&#125;<br><br><span class="hljs-comment">// 可能会出现队列当中的数要小于窗口长度, 因为上一个循环弹出的数太多了也有可能</span><br>q.<span class="hljs-built_in">push_back</span>(i);<br><span class="hljs-keyword">if</span>(q.<span class="hljs-built_in">front</span>() &lt; i-k<span class="hljs-number">+1</span>)&#123;<br>q.<span class="hljs-built_in">pop_front</span>();<br>&#125;<br><br><span class="hljs-keyword">if</span>(i &gt;= k<span class="hljs-number">-1</span>)<br>ans.<span class="hljs-built_in">push_back</span>(nums[q[<span class="hljs-number">0</span>]]);<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="最小窗口"><a href="#最小窗口" class="headerlink" title="最小窗口"></a>最小窗口</h2><p>找到串中包含目标穿的最小子串，窗口就必然是要伸缩的。为了确认重复字符，数量也需要考虑。哈希表维护目标串和当前窗口内字符种类和数量，右指针不断移动，在找到了包含完整的一个目标串后，左指针尝试能不能右移，更新最小值。注意维护窗口的哈希表</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs c++"> <span class="hljs-keyword">while</span>(r &lt; s.<span class="hljs-built_in">size</span>())&#123;<br>cnt[s[r]]++;<br><br><span class="hljs-keyword">while</span>(<span class="hljs-built_in">check</span>() &amp;&amp; l &lt;= r)&#123;<br><span class="hljs-keyword">if</span>(r-l<span class="hljs-number">+1</span> &lt; len)&#123;<br>len = r - l + <span class="hljs-number">1</span>;<br>ans = l;<br>&#125;<br><br>cnt[s[l]]--;<br>l++;<br>&#125;<br>r++;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="最小覆盖子串"><a href="#最小覆盖子串" class="headerlink" title="最小覆盖子串"></a>最小覆盖子串</h2><p>给你一个字符串 <code>s</code> 、一个字符串 <code>t</code> 。返回 <code>s</code> 中涵盖 <code>t</code> 所有字符的最小子串。如果 <code>s</code> 中不存在涵盖 <code>t</code> 所有字符的子串，则返回空字符串 <code>&quot;&quot;</code> 。</p><p>还是用哈希表存储目标串和窗口串的字符种类和数量，因为需要考虑到重复字符的出现所以必须是哈希 map。算法是首先找到一个包含目标串的子串（如果不满足 check 函数的话右边界会一直向右）然后在不断尝试缩小窗口（ <strong>重要，否则左边界没办法移动，不可以找到一个极值之后直接跳跃左边界</strong>） ，每一次缩小窗口都会减去窗口哈希表里面的项，同时更新最小长度。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">while</span>(r &lt; s.<span class="hljs-built_in">size</span>())&#123;<br>cnt[s[r]]++;<br><br><span class="hljs-keyword">while</span>(<span class="hljs-built_in">check</span>() &amp;&amp; l &lt;= r)&#123;<br><span class="hljs-keyword">if</span>(r-l<span class="hljs-number">+1</span> &lt; len)&#123;<br>len = r - l + <span class="hljs-number">1</span>;<br>ans = l;<br>&#125;<br><br>cnt[s[l]]--;<br>l++;<br>&#125;<br>r++;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="和为-K-的子数组"><a href="#和为-K-的子数组" class="headerlink" title="和为 K 的子数组"></a>和为 K 的子数组</h2><p>给你一个整数数组 <code>nums</code> 和一个整数 <code>k</code> ，请你统计并返回 <em>该数组中和为 <code>k</code> 的子数组的个数</em> 。</p><p>简单的我们可以用前缀和数组查找整个数组中某一段子数组是不是存在 sum &#x3D;&#x3D; K 的情况，由于每一次都是检查任意的中间一段所以这种遍历是全局的，没有遗漏。时间复杂度是 $O(n^2)$ ，也可以说这是一种窗口。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= nums.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j = i; j&lt;= nums.<span class="hljs-built_in">size</span>(); j++)&#123;<br><span class="hljs-type">int</span> m = sum[j] - sum[i<span class="hljs-number">-1</span>];<br><br><span class="hljs-keyword">if</span>(m == k)<br>cnt++;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>或者可以用哈希表来简化查找<em>是否存在</em>这个步骤，保留每个前缀在哈希表中的 count，如果发现 <code>mp.find(pre - k) != mp.end()</code> 那么就表示中间的一段 sum &#x3D;&#x3D; K。子数组的解是可以复用的，边遍历边查找。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">subarraySum</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> k)</span> </span>&#123;<br>unordered_map&lt;<span class="hljs-type">int</span>, <span class="hljs-type">int</span>&gt; mp;<br>mp[<span class="hljs-number">0</span>] = <span class="hljs-number">1</span>;<br><span class="hljs-type">int</span> count = <span class="hljs-number">0</span>, pre = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-keyword">auto</span>&amp; x:nums) &#123;<br>pre += x;<br><span class="hljs-keyword">if</span> (mp.<span class="hljs-built_in">find</span>(pre - k) != mp.<span class="hljs-built_in">end</span>()) &#123;<br>count += mp[pre - k];<br>&#125;<br>mp[pre]++;<br>&#125;<br><span class="hljs-keyword">return</span> count;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="树"><a href="#树" class="headerlink" title="树"></a>树</h1><h2 id="二叉树中的最大路径和"><a href="#二叉树中的最大路径和" class="headerlink" title="二叉树中的最大路径和"></a>二叉树中的最大路径和</h2><p>找到二叉树中的最大路径和，抽象出一个小二叉树，全局的最大路径可能是包含了顶点的，也可能是路径的一个边不包含顶点。所以对于全局来说，要更新的是 maxLen &#x3D; max(maxLen, node-&gt;val+left+right) ，递归到某一层的时候，不能把一个经过顶点的路径交给上层，也就是上层不能在下面分叉处两条路，所以是 return node-&gt;val + max(left, right) 。注意这里可能出现左右子树的最长路径和为负数，要及时终止：找的路径不一定是要从最下面层开始，递归只返回一个节点也是可以的。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">maxhelper</span><span class="hljs-params">(TreeNode* node)</span></span>&#123;<br><span class="hljs-keyword">if</span>(!node)<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br><span class="hljs-keyword">if</span>(node-&gt;left == <span class="hljs-literal">nullptr</span> &amp;&amp; node-&gt;right == <span class="hljs-literal">nullptr</span>)&#123;<br>maxLen = <span class="hljs-built_in">max</span>(node-&gt;val, maxLen);<br><span class="hljs-keyword">return</span> node-&gt;val;<br>&#125;<br><span class="hljs-type">int</span> left = <span class="hljs-built_in">max</span>(<span class="hljs-number">0</span>, <span class="hljs-built_in">maxhelper</span>(node-&gt;left));<br><span class="hljs-type">int</span> right = <span class="hljs-built_in">max</span>(<span class="hljs-number">0</span>, <span class="hljs-built_in">maxhelper</span>(node-&gt;right));<br><br>maxLen = <span class="hljs-built_in">max</span>(maxLen, node-&gt;val+left+right);<br><span class="hljs-keyword">return</span> node-&gt;val + <span class="hljs-built_in">max</span>(left, right);    <span class="hljs-comment">// 也可能子节点都是负数，不选子节点也可以</span><br>&#125;<br></code></pre></td></tr></table></figure><h1 id="图论"><a href="#图论" class="headerlink" title="图论"></a>图论</h1><p>DFS 或者 BFS</p><h2 id="岛屿数量"><a href="#岛屿数量" class="headerlink" title="岛屿数量"></a>岛屿数量</h2><p>这道题用 DFS 的方法解决，依次遍历二维网格的每一个点，如果发现当前是岛屿，那么就继续向下探索其他附近的岛屿。一直不断递归。如果是岛屿那么就把网格当中这个格子给重新标记，注意这里是一个不会被使用的值。如果 DFS<strong>超出了数组范围的话或者识别当前已经到了岛屿的水</strong>，那么就应该返回。这样回到最初的遍历所有和岛屿相关的格子都已经被标记过，不会重复叠加，这样就可以数出正确的岛屿。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">numIslands</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">char</span>&gt;&gt;&amp; grid)</span> </span>&#123;<br><span class="hljs-type">int</span> n = grid.<span class="hljs-built_in">size</span>(), m = grid[<span class="hljs-number">0</span>].<span class="hljs-built_in">size</span>();<br><span class="hljs-type">int</span> count = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; grid.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; grid[<span class="hljs-number">0</span>].<span class="hljs-built_in">size</span>(); j++)&#123;<br><span class="hljs-keyword">if</span>(grid[i][j] == <span class="hljs-string">&#x27;1&#x27;</span>)&#123;<br>count++;<br><span class="hljs-built_in">dfs</span>(grid, i ,j);<br>&#125;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> count;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">dfs</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">char</span>&gt;&gt;&amp; grid, <span class="hljs-type">int</span> x, <span class="hljs-type">int</span> y)</span></span>&#123;<br><span class="hljs-keyword">if</span>(x &lt; <span class="hljs-number">0</span> || y &lt; <span class="hljs-number">0</span> || x &gt;= grid.<span class="hljs-built_in">size</span>() || y &gt;= grid[<span class="hljs-number">0</span>].<span class="hljs-built_in">size</span>() || grid[x][y] != <span class="hljs-string">&#x27;1&#x27;</span>)<br><span class="hljs-keyword">return</span> ;<br>grid[x][y] = <span class="hljs-string">&#x27;#&#x27;</span>;<br><span class="hljs-built_in">dfs</span>(grid, x<span class="hljs-number">+1</span>, y);<br><span class="hljs-built_in">dfs</span>(grid, x<span class="hljs-number">-1</span>, y);<br><span class="hljs-built_in">dfs</span>(grid, x, y<span class="hljs-number">+1</span>);<br><span class="hljs-built_in">dfs</span>(grid, x, y<span class="hljs-number">-1</span>);<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="腐烂的橘子"><a href="#腐烂的橘子" class="headerlink" title="腐烂的橘子"></a>腐烂的橘子</h2><p>这道题的要求是输出腐烂的时间数，所以可以用每一轮的腐烂来模拟，是一个 BFS。需要注意每一次处理腐烂的是上一轮新增的腐烂橘子，<strong>只有在这一轮腐烂的橘子都处理完之后时间数才会自增</strong>。直到没有新增的腐烂橘子为止。队列当中存放的是腐烂的橘子，代码执行的时候队列里的元素应该是增长又减少的。<br>gpt 写的比我好，四个方向的处理很优雅。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs c++">std::array&lt;std::pair&lt;<span class="hljs-type">int</span>, <span class="hljs-type">int</span>&gt;, 4&gt; directions = &#123;&#123;&#123;<span class="hljs-number">0</span>, <span class="hljs-number">1</span>&#125;, &#123;<span class="hljs-number">1</span>, <span class="hljs-number">0</span>&#125;, &#123;<span class="hljs-number">0</span>, <span class="hljs-number">-1</span>&#125;, &#123;<span class="hljs-number">-1</span>, <span class="hljs-number">0</span>&#125;&#125;&#125;;<br>queue&lt;pair&lt;<span class="hljs-type">int</span>, <span class="hljs-type">int</span>&gt;&gt; rote;<br><span class="hljs-keyword">while</span>(!rote.<span class="hljs-built_in">empty</span>()) &#123;<br><span class="hljs-type">int</span> size = rote.<span class="hljs-built_in">size</span>();<br><span class="hljs-type">bool</span> rottedThisRound = <span class="hljs-literal">false</span>;<br><br><span class="hljs-keyword">while</span>(size--) &#123;<br><span class="hljs-keyword">auto</span> [x, y] = rote.<span class="hljs-built_in">front</span>();<br>rote.<span class="hljs-built_in">pop</span>();<br><br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">auto</span> [dx, dy] : directions) &#123;<br><span class="hljs-type">int</span> nx = x + dx, ny = y + dy;<br><span class="hljs-keyword">if</span>(nx &gt;= <span class="hljs-number">0</span> &amp;&amp; ny &gt;= <span class="hljs-number">0</span> &amp;&amp; nx &lt; m &amp;&amp; ny &lt; n &amp;&amp; grid[nx][ny] == <span class="hljs-number">1</span>) &#123;<br>grid[nx][ny] = <span class="hljs-number">2</span>; <span class="hljs-comment">// 腐烂新鲜橘子</span><br>rote.<span class="hljs-built_in">emplace</span>(nx, ny);<br>freshCount--;<br>rottedThisRound = <span class="hljs-literal">true</span>;<br>&#125;<br>&#125;<br>&#125;<br><br><span class="hljs-keyword">if</span>(rottedThisRound) minute++;  <span class="hljs-comment">// 只有实际腐烂时才增加时间</span><br>&#125;<br><span class="hljs-comment">// 还有新鲜橘子没腐烂到</span><br><span class="hljs-keyword">return</span> freshCount == <span class="hljs-number">0</span> ? minute : <span class="hljs-number">-1</span>;<br></code></pre></td></tr></table></figure><h2 id="课程表"><a href="#课程表" class="headerlink" title="课程表"></a>课程表</h2><p>思想是检查一个数组链当中会不会出现环，由于每一次都需要对前置课程进行确认，所以不断的前置就构成了 DFS。访问过的课程标记，并且加上了优化（如果确认到达某一个课程之前的所有课程都不会出现依赖闭环，那么就可以在这里截止）。<br>初始化访问数组为0，表示没有检查这个课程；1 表示开始检查此课程，接下来要对所有的依赖课程检查，并且不断递归。如果检查到了合法的课程，那么最后的 for 遍历应该会顺利结束（因为最后一个课程不会依赖一个环，会直接到 <code>visited[course] = 2</code> ）。最后依次返回，2 表示访问的课程已经检查过了是合法的</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">visited</span><span class="hljs-params">(numCourses, <span class="hljs-number">0</span>)</span></span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; numCourses; i++)&#123;<br><span class="hljs-keyword">if</span>(!<span class="hljs-built_in">dfs</span>(graph, i, visited))  <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br><br>&#125;<br>......<br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">dfs</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; graph, <span class="hljs-type">int</span> course, vector&lt;<span class="hljs-type">int</span>&gt;&amp; visited)</span></span>&#123;<br><span class="hljs-keyword">if</span>(visited[course] == <span class="hljs-number">1</span>)    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br><span class="hljs-keyword">if</span>(visited[course] == <span class="hljs-number">2</span>)    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;  <span class="hljs-comment">// 提前返回，优化</span><br><br>visited[course] = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-keyword">auto</span> c : graph[course])&#123;<br><span class="hljs-keyword">if</span>(!<span class="hljs-built_in">dfs</span>(graph, c, visited))     <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br><br>&#125;<br>visited[course] = <span class="hljs-number">2</span>;<br><span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="动态规划-1"><a href="#动态规划-1" class="headerlink" title="动态规划"></a>动态规划</h1><h2 id="最长有效匹配括号"><a href="#最长有效匹配括号" class="headerlink" title="最长有效匹配括号"></a>最长有效匹配括号</h2><p>给你一个只包含 <code>&#39;(&#39;</code> 和 <code>&#39;)&#39;</code> 的字符串，找出最长有效（格式正确且连续）括号子串的长度。</p><p>用的是栈的方法。栈记录的是索引，每当发生一次 () 匹配，就应当更新一下最长的长度 <code>i-st.top()</code>，其中 i 是移动的游标，栈顶表示最远没有匹配的 <code>(</code> ，两者的差就是一次可能的最大值。这里还要加上一个分界节点，如果起点开始就是 <code>)</code> ，或者栈中的符号都被匹配清空了，开始下一轮结果开始碰到了 <code>(</code> 那应该要及时跳过。所以可以理解一下，多个匹配串中间夹着不能匹配的之后又碰到了，就是这个起点不断更新的过程。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c++">st.<span class="hljs-built_in">push</span>(<span class="hljs-number">-1</span>);<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; s.<span class="hljs-built_in">length</span>(); i++)&#123;<br><span class="hljs-keyword">if</span>(s[i] == <span class="hljs-string">&#x27;(&#x27;</span>)<br>st.<span class="hljs-built_in">push</span>(i);<br><span class="hljs-keyword">else</span>&#123;<br>   st.<span class="hljs-built_in">pop</span>();<br>   <span class="hljs-keyword">if</span>(st.<span class="hljs-built_in">empty</span>())<br>st.<span class="hljs-built_in">push</span>(i); <br><span class="hljs-keyword">else</span>&#123;<br>maxlen = <span class="hljs-built_in">max</span>(maxlen, i-st.<span class="hljs-built_in">top</span>());<br>&#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="回溯"><a href="#回溯" class="headerlink" title="回溯"></a>回溯</h1><h2 id="全排列"><a href="#全排列" class="headerlink" title="全排列"></a>全排列</h2><p>回溯本质上还是一个递归的算法，终止条件是：如果找到的路径元素个数是原数组个数的话那么就把它加到结果集里面。每一次遍历的时候还要维护一个只用数组因为这里要求的是不重复元素，所以已经添加的路径元素不能在下一次递归的时候使用，需要用一个全局的使用数组标记。传入的是一个空路径然后每一次递归的时候添加一个元素进去，直到元素满了中止。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtrack</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt; &amp;nums, vector&lt;<span class="hljs-type">int</span>&gt; &amp;path, vector&lt;<span class="hljs-type">bool</span>&gt; &amp;used, vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt; &amp;result)</span></span>&#123;<br><span class="hljs-keyword">if</span>(path.<span class="hljs-built_in">size</span>() == nums.<span class="hljs-built_in">size</span>())&#123;<br>result.<span class="hljs-built_in">push_back</span>(path);<br><span class="hljs-keyword">return</span> ;<br>&#125;<br><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-keyword">if</span>(used[i]) <span class="hljs-keyword">continue</span>;<br><br>path.<span class="hljs-built_in">push_back</span>(nums[i]);<br>used[i] = <span class="hljs-literal">true</span>;<br><br><span class="hljs-built_in">backtrack</span>(nums, path, used, result);<br><br>path.<span class="hljs-built_in">pop_back</span>();<br>used[i] = <span class="hljs-literal">false</span>;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="子集"><a href="#子集" class="headerlink" title="子集"></a>子集</h2><p>这些问题和全排列不同是在搜索的过程当中每一个中间的结果都需要加到结果集里面，他改变的一点是每一次自己只是在后面的元素当中去找，不会向前搜索已经遍历过的元素。<strong><em><code>[2,1]</code> 和 <code>[1,2]</code>是重复的，但是<code>[1,2]</code> 和 <code>[2,1]</code> 是不同的排列！</em></strong> 所以子集问题传入了一个开始下标 (这种也是一种剪枝)</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtrack</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt; &amp;nums, vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt; &amp;result, vector&lt;<span class="hljs-type">int</span>&gt; path, <span class="hljs-type">int</span> startIndex)</span></span>&#123;  <span class="hljs-comment">//这里是下标</span><br>result.<span class="hljs-built_in">push_back</span>(path);<br><span class="hljs-keyword">if</span>(path.<span class="hljs-built_in">size</span>() == nums.<span class="hljs-built_in">size</span>())  <span class="hljs-keyword">return</span> ;<br><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = startIndex; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)&#123;<br>path.<span class="hljs-built_in">push_back</span>(nums[i]);<br><br><span class="hljs-built_in">backtrack</span>(nums, result, path, i<span class="hljs-number">+1</span>);<br><br>path.<span class="hljs-built_in">pop_back</span>();<br><br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="电话号码的组合"><a href="#电话号码的组合" class="headerlink" title="电话号码的组合"></a>电话号码的组合</h2><p>总体还是一个子集的思路，就是不能往前看，读取到下一个数字的时候不会在读取之前数字对应的字符集。终止条件是如果添加的字符长度等于输入的数字，因为每一个输入的数字都会对应一个字符，那么就可以添加到结果里。注意这里是有两层循环的，第一层是依次对数字循环，第二层是对数字对应的字符集</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtrack</span><span class="hljs-params">(unordered_map&lt;<span class="hljs-type">char</span>, string&gt; &amp;map, string &amp;digits, string &amp;path, vector&lt;string&gt; &amp;result, <span class="hljs-type">int</span> startIndex)</span></span>&#123;<br><span class="hljs-keyword">if</span>(digits.<span class="hljs-built_in">length</span>() == path.<span class="hljs-built_in">length</span>())&#123;<br><span class="hljs-keyword">if</span>(digits.<span class="hljs-built_in">length</span>() != <span class="hljs-number">0</span>)<br>result.<span class="hljs-built_in">push_back</span>(path);<br><span class="hljs-keyword">return</span>;<br>&#125;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = startIndex; i &lt; digits.<span class="hljs-built_in">length</span>(); i++)&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">char</span> c : map[digits[i]])&#123;<br>path.<span class="hljs-built_in">push_back</span>(c);<br><span class="hljs-built_in">backtrack</span>(map, digits, path, result, i<span class="hljs-number">+1</span>);<br>path.<span class="hljs-built_in">pop_back</span>();<br>&#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="组合总和"><a href="#组合总和" class="headerlink" title="组合总和"></a>组合总和</h2><p>递归很容易想到，只要一直去减列表数组里的数就可以。然后下一次迭代的时候，传入的目标数是减过后的数。这道题不一样的地方是，它一个数字可以用多次，但还是需要去掉重复组合。所以还是子集做题的思路，不过这一次不需要从下一个数开始找，从当前数找就可以，因为可以用多次。终止的条件是，如果目标数被减到了零，那么说明这个组合的和就是 target；如果小于零那么这个搜索就不成立直接返回。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtrack</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; candidate, <span class="hljs-type">int</span> target, vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; result, vector&lt;<span class="hljs-type">int</span>&gt; path, <span class="hljs-type">int</span> start)</span></span>&#123;<br><span class="hljs-keyword">if</span>(target == <span class="hljs-number">0</span>)&#123;<br>result.<span class="hljs-built_in">push_back</span>(path);<br><span class="hljs-keyword">return</span> ;<br>&#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(target &lt; <span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> ;<br><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = start; i &lt; candidate.<span class="hljs-built_in">size</span>(); i++)&#123;<br><span class="hljs-type">int</span> num = candidate[i];<br>path.<span class="hljs-built_in">push_back</span>(num);<br><span class="hljs-comment">// target -= num;</span><br><span class="hljs-built_in">backtrack</span>(candidate, target-num, result, path, i);<br>path.<span class="hljs-built_in">pop_back</span>();<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="单词搜索"><a href="#单词搜索" class="headerlink" title="单词搜索"></a>单词搜索</h2><p>从找到的第一个字符找到开始继续向下搜索，只能上下左右搜索，带着下标一起，如果下标达到了目标字符串的长度就确定找到，立刻停止所有地搜索（剪枝优化）。注意不能重复使用之前的格子，可能会绕了一圈回到之前的，虽然本来没有带入原下标搜索，所以每一个搜索的位置都要标记，搜完了再回填。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtrack</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">char</span>&gt;&gt;&amp; board, string word, <span class="hljs-type">int</span> x, <span class="hljs-type">int</span> y, <span class="hljs-type">int</span> index)</span></span>&#123;<br><span class="hljs-keyword">if</span>(ans) <span class="hljs-keyword">return</span>;  <span class="hljs-comment">// 全局的，一旦某个搜索找到，立刻停止所有递归</span><br><span class="hljs-keyword">if</span>(x &gt;= board.<span class="hljs-built_in">size</span>() || y &gt;= board[<span class="hljs-number">0</span>].<span class="hljs-built_in">size</span>() || x &lt; <span class="hljs-number">0</span> || y &lt; <span class="hljs-number">0</span> || board[x][y] != word[index])<br><span class="hljs-keyword">return</span> ;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(index == word.<span class="hljs-built_in">size</span>()<span class="hljs-number">-1</span> &amp;&amp; board[x][y] == word[index])&#123;<br>ans = <span class="hljs-literal">true</span>;<br><span class="hljs-keyword">return</span> ;<br>&#125;<span class="hljs-keyword">else</span>&#123;<br><span class="hljs-type">char</span> tmp = board[x][y];<br>board[x][y] = <span class="hljs-string">&#x27;#&#x27;</span>;<br><span class="hljs-built_in">backtrack</span>(board, word, x<span class="hljs-number">+1</span>, y, index<span class="hljs-number">+1</span>);<br><span class="hljs-built_in">backtrack</span>(board, word, x, y<span class="hljs-number">+1</span>, index<span class="hljs-number">+1</span>);<br><span class="hljs-built_in">backtrack</span>(board, word, x<span class="hljs-number">-1</span>, y, index<span class="hljs-number">+1</span>);<br><span class="hljs-built_in">backtrack</span>(board, word, x, y<span class="hljs-number">-1</span>, index<span class="hljs-number">+1</span>);<br>board[x][y] = tmp;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="分割回文串"><a href="#分割回文串" class="headerlink" title="分割回文串"></a>分割回文串</h2><p>思路类似，依次从字符串的开始遍历，字串长度不断递增，如果发现前面的串是回文串，就把后面的字符串分割出来传递给下一个继续迭代。回溯体现在一个搜索结束之后，是要把他之前的每一步向下搜索都要抹去，这样才能记录所有其他的路径。最后传递的是空串，搜索结束。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtrack</span><span class="hljs-params">(string s, vector&lt;vector&lt;string&gt;&gt;&amp; result, vector&lt;string&gt;&amp; path)</span></span>&#123;<br><span class="hljs-comment">// 怎么处理结束呢</span><br><span class="hljs-keyword">if</span>(s.<span class="hljs-built_in">empty</span>())&#123;<br>result.<span class="hljs-built_in">push_back</span>(path);<br><span class="hljs-keyword">return</span> ;<br>&#125;<br><br><span class="hljs-type">int</span> len = s.<span class="hljs-built_in">length</span>();<br><span class="hljs-comment">// 如果substr(s.length)会返回一个空字符串</span><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= s.<span class="hljs-built_in">length</span>(); i++)&#123;<br><span class="hljs-keyword">if</span>(<span class="hljs-built_in">check_palindrome</span>(s.<span class="hljs-built_in">substr</span>(<span class="hljs-number">0</span>, i)))&#123;<br>path.<span class="hljs-built_in">push_back</span>(s.<span class="hljs-built_in">substr</span>(<span class="hljs-number">0</span>, i));<br><span class="hljs-built_in">backtrack</span>(s.<span class="hljs-built_in">substr</span>(i, len-i), result, path);<br>path.<span class="hljs-built_in">pop_back</span>();  <span class="hljs-comment">// 回溯</span><br>&#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="N-皇后"><a href="#N-皇后" class="headerlink" title="N 皇后"></a>N 皇后</h2><p>还是回溯的思路，一层一层的判断，如果到达了最后一层，那么就表示找到了一种符合 N 皇后的排列局面。所以只需要对每一个 col 进行遍历看能否放皇后，如果可以就进入下一层，然后再消除痕迹。这里比较巧妙的就是判断了，行列对角线。行不会出现重复，列、对角线需要的。这里用的是哈希表维护每一个局面的用到的列、对角线，在返回上级的时候也需要擦除痕迹。</p><blockquote><p><em>写的时候还要注意，string 是一行，<code>vector&lt;string&gt;</code>代表一个局面</em></p></blockquote><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">dfs</span><span class="hljs-params">(<span class="hljs-type">int</span> row, vector&lt;string&gt;&amp; board)</span></span>&#123;<br><span class="hljs-keyword">if</span>(row == N)&#123;<br>ans.<span class="hljs-built_in">push_back</span>(board);<br><span class="hljs-keyword">return</span> ;<br>&#125;<br><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; N; j++)&#123;<br><span class="hljs-keyword">if</span>(<span class="hljs-built_in">check</span>(row, j))&#123;<br>board[row][j] = <span class="hljs-string">&#x27;Q&#x27;</span>;<br>colMark.<span class="hljs-built_in">insert</span>(j), masterMark.<span class="hljs-built_in">insert</span>(row-j), secondMark.<span class="hljs-built_in">insert</span>(row+j);<br><span class="hljs-built_in">dfs</span>(row<span class="hljs-number">+1</span>, board);<br>colMark.<span class="hljs-built_in">erase</span>(j), masterMark.<span class="hljs-built_in">erase</span>(row-j), secondMark.<span class="hljs-built_in">erase</span>(row+j);<br>board[row][j] = <span class="hljs-string">&#x27;.&#x27;</span>;<br>&#125;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="技巧"><a href="#技巧" class="headerlink" title="技巧"></a>技巧</h1><h2 id="只出现一次的数字"><a href="#只出现一次的数字" class="headerlink" title="只出现一次的数字"></a>只出现一次的数字</h2><p>位运算</p><h2 id="多数元素"><a href="#多数元素" class="headerlink" title="多数元素"></a>多数元素</h2><p>简单哈希表，不需要用迭代器判断是否为空，kv map 可以直接 <code>map[i] = num</code>, 也可以简化边插入边判断</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c++"> <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> num: nums)&#123;<br>map[num]++;<br><span class="hljs-keyword">if</span>(map[num] &gt; half)&#123;<br>ans = num;<br><span class="hljs-keyword">break</span>;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="颜色分类"><a href="#颜色分类" class="headerlink" title="颜色分类"></a>颜色分类</h2><p>三个数字的排序问题但是这里要求原地进行更改所以用三指针方法，左指针左边的是 0，右指针右边的是 2，中间是待整理的。遍历整个数组进行排序：和 high 减缓的时候 i 不能移动，此时还不清楚 i 之后的位置元素，没有被整理，所以交换来的还需要再次检查，i 不动。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> low = <span class="hljs-number">0</span>, high = nums.<span class="hljs-built_in">size</span>()<span class="hljs-number">-1</span>;<br><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; high;)&#123;<br><span class="hljs-keyword">if</span>(nums[i] == <span class="hljs-number">0</span>)&#123;<br><span class="hljs-built_in">swap</span>(nums[i], nums[low]);<br>low++;<br>i++;<br>&#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(nums[i] == <span class="hljs-number">1</span>)&#123;<br>i++;<br>&#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(nums[i] == <span class="hljs-number">2</span>)&#123;<br><span class="hljs-built_in">swap</span>(nums[i], nums[high]);<br>high--;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="下一个排列"><a href="#下一个排列" class="headerlink" title="下一个排列"></a>下一个排列</h2><p>从数组最后开始扫描，目标是找到递减序列，然后之前的一个拐点，后面都是递减序列的话，怎么调变得更小，不会变大。所以找到拐点之后，和后面的比他稍大一点的数交换，<strong>并且把后面的递减序列改成递增序列</strong>，这样就是最小的下一个数。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = nums.<span class="hljs-built_in">size</span>()<span class="hljs-number">-2</span>; i &gt;= <span class="hljs-number">0</span>; i--)&#123;<br><span class="hljs-keyword">if</span>(nums[i] &lt; nums[i<span class="hljs-number">+1</span>])&#123;<br>turn = i;<br><span class="hljs-keyword">break</span>;<br>&#125;<br>&#125;<br><span class="hljs-keyword">if</span>(turn != <span class="hljs-number">-1</span>)&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = nums.<span class="hljs-built_in">size</span>()<span class="hljs-number">-1</span>; i &gt; turn; i--)&#123;<br><span class="hljs-keyword">if</span>(nums[i] &gt; nums[turn])&#123;<br>next = i;<br><span class="hljs-keyword">break</span>;<br>&#125;<br>&#125;<br>&#125;<br><br><span class="hljs-keyword">if</span>(turn != <span class="hljs-number">-1</span> &amp;&amp; next != <span class="hljs-number">-1</span>)&#123;<br><span class="hljs-built_in">swap</span>(nums[turn], nums[next]);<br><span class="hljs-built_in">reverse</span>(nums.<span class="hljs-built_in">begin</span>()+turn<span class="hljs-number">+1</span>, nums.<span class="hljs-built_in">end</span>());<br>&#125;<span class="hljs-keyword">else</span>&#123;<br><span class="hljs-built_in">reverse</span>(nums.<span class="hljs-built_in">begin</span>(), nums.<span class="hljs-built_in">end</span>());<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="寻找重复数"><a href="#寻找重复数" class="headerlink" title="寻找重复数"></a>寻找重复数</h2><p>把数组看成一个链表，不重复的数字就是每一个不同的节点，重复的数字相当于有环，用检测环的快慢指针法可以找到。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> fast = <span class="hljs-number">0</span>, slow = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">do</span>&#123;<br>slow = nums[slow];<br>fast = nums[nums[fast]];<br>&#125;<span class="hljs-keyword">while</span>(slow != fast);<br><br>slow = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">do</span>&#123;<br>slow = nums[slow];<br>fast = nums[fast];<br>&#125;<span class="hljs-keyword">while</span>(slow != fast);<br><br><span class="hljs-keyword">return</span> slow;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>技术笔记</category>
      
      <category>算法题</category>
      
    </categories>
    
    
    <tags>
      
      <tag>算法</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>JavaEE-1</title>
    <link href="/javaee/1/"/>
    <url>/javaee/1/</url>
    
    <content type="html"><![CDATA[<div class="note note-danger">                        <div class="note-content">              <p><em><strong>javaee课程来自听课的记录，用词口语，可能出现不少的错字</strong></em>  </p>            </div>          </div>  <h1 id="预备知识"><a href="#预备知识" class="headerlink" title="预备知识"></a>预备知识</h1><h2 id="url-servlet"><a href="#url-servlet" class="headerlink" title="url servlet"></a>url servlet</h2><p><img src="/img/javaee-1/f66e70f0bcccc28f5701a5eb04b3843e.png"><br>dns 查名服务类似于spring微服务里面的Nacos服务发现，也是一个服务对应的IP地址。不过它提供了更多的类似于负载均衡，动态上下线，查看健康状况等等的功能，更加智能。</p><p>HTTP或者https协议都是一问一答的形式，没有问就没有答，服务器没有保存上下文的信息。所有的内容都应该包含在request里面</p><p><img src="/img/javaee-1/cd19197089f1265f523746214bde383b.png"><br>符合javaee规范的servlet容器组件规范。服务端把请求request和响应发送到servlet基类，然后会调用service接口。HTTP servlet实现了这个基类就可以重写其中的doget dopost等等方法，这部分是自己的代码，把内容写到响应response， servlet再把这个响应送到前端服务器去响应渲染出一个页面。用的比较多的符合Java EE规范的servlet容器是tomcat，或者可以使用jetty。<br>使用规范的好处是，如果想要更换容器的话不需要再重新写这些接口的方法。</p><p><img src="/img/javaee-1/b7e3c80190a437e724b2706a17a58bfc.png"><br>servlet对象只有一个，每一个请求都会用一个单独的线程去跑这个对象没有属性，因为线程会共享对象的属性会造成冲突 。 read的过程就是TCP的数据包拼接，decode把接收到的网络数据包分成一个Java对象，computer是自己service方法的处理逻辑。<br>但是线程不能无限制地增长，会受到内存CPU的限制。可以事先用线程池，线程池就是一个限制了可以使用的最大线程数，多于这个最大容量就会等待；如果超过了限制的等待时间，那么就会被拒绝。同时还会在预先的时候分配一定的孵化线程，有需求的时候就拿出来用。<br>还需要分析任务是io密集型还是CPU密集型，如果是io密集型，那可以多开线程消耗内存会比较大，但是CPU用的不是很多；如果是CPU密集型，那么即使线程数开得很多CPU的上限也不能突破。</p><img src = "/img/javaee-1/92da6a8a3e095712b3a55d3022f3c21b.png" width = "400">  <h2 id="maven"><a href="#maven" class="headerlink" title="maven"></a>maven</h2><p>工程文件依赖的第三方jar包，以及提供中立的工具管理。</p><img src = "/img/javaee-1/42e885503482eccf3552c82a86eade07.png" width = "400">  <p>插件才是真正执行操作的</p><h1 id="领域建模思想"><a href="#领域建模思想" class="headerlink" title="领域建模思想"></a>领域建模思想</h1><p><img src="/img/javaee-1/40c183e6f64959811592ca77bdce9489.png"><br><strong>一个概念在不同的视角下可能会有不同的属性</strong>，衍生不同的概念<br>局部和整体的关系，如果修改了商品，那么销售的概念也可能会改变；商铺被禁止了，那么商品和销售都会联动的被禁止。这些概念之间还存在各种各样的关系一对一或者一对多、自己和自己的关系（捆绑一对多、相关商品）<br>这里商品和销售的概念非常密切，所以没有把下面销售的概念和上面商品实体的概念分割开。</p><h1 id="Mybatis"><a href="#Mybatis" class="headerlink" title="Mybatis"></a>Mybatis</h1><h1 id="Mysql原理"><a href="#Mysql原理" class="headerlink" title="Mysql原理"></a>Mysql原理</h1><p>关注的是大数据高并发的情况下Mysql里面的存储和逻辑结构：<br><img src="/img/javaee-1/9d705b796f53be1ed34a923043123919.png"></p><ul><li>在物理结构之下实际的存储是分为tablespace segment extent和page，需要关注的是page里面包含了实际的表数据结构， page directory可以根据要查找的主键进行二分查找快速查询。 page之间是一个双向链表的关系，page directory是单链表结构<br><img src="/img/javaee-1/7b32ecf3ea2ed7d5aeada6cb6af8779b.png"></li></ul><p>这里要做一个区分，在逻辑结构下索引的存储叶子节点存放了实际的记录，也就是page。但是在物理结构之下，索引同样存放在物理的page下，它的索引是每一个页的最小主键值。根据要查找的所主键，找到存放的页的号码，根据这个页的号码，去extent找到对应的页，page directory二分查找目标主键值。这也是为什么叫做聚簇索引，把索引值和实际的记录绑定在一起。</p><p>page directory是定长的，我们要让这个数量中和一下。如果page太小，那么磁盘读出来的数据转一圈也很少，会有寻道时间的损失。<br>从磁盘当中根据索引读出来记录也是一个page，这是磁盘转一圈读到的数据大小，把它放在内存之后会根据哈希索引保存key value下次要查询某一个主键直接去读对应的value就可以了<br>做辅助索引会产生比主键递增索引更多的插入分裂（因为辅助索引的插入都是随机的，大概率还是在中间插入）。所以仅在必要的外键上面做辅助索引，如果要查询特殊的名称的话，就不要在关系型数据库里面做。<em>这里也注意，如果要做查询的话一定要有索引</em><br><img src="/img/javaee-1/62a4bc19c4aa64f817701f67037670a4.png"><br>中间插入节点可能会使节点分裂，设置主键自增在最后插入数据才是减少分裂的最好插入方式，只会影响最后一页<br>删除节点更麻烦，它可能是中间某一层的分割节点。需要重新合并，实际上做的是逻辑的删除不是物理的删除</p><p>这个图还是比较清楚的，把创建readview和update记录的db_trx_id进行比较就可以实现读问题<img src="/img/javaee-1/56c714d1b2e27b8c55617d3337b9e589.png"><br>mvcc多并发版本读，解决了多个事务开启下的读问题，但是不能解决写，写还是互斥的。undolog链历史记录在所有的依赖事务结束之后会被删除，在创建记录的事务被提交之后打上history标记。</p><h1 id="spring-微服务"><a href="#spring-微服务" class="headerlink" title="spring 微服务"></a>spring 微服务</h1><h2 id="原则"><a href="#原则" class="headerlink" title="原则"></a>原则</h2><p><img src="/img/javaee-1/188eeea25d2023d81a7e656781c4807d.png"><br>单一职责：一个微服务解决一个问题，但实际业务开发采用了<strong>一个领域之间的松散耦合程度</strong>来判断是不是可以聚合成一个微服务。<br>松散耦合：采用轻量级通信（RESTful API 调用服务之间的功能）<br>独立存在：独立数据结构和数据库，每一个微服务都可以有自己的数据库（Mysql，Oracle……），甚至redis</p><p>为什么不一个微服务只用两张表或者三张表？<br>对象之间没有办法关联了，面向对象发挥不出威力，职责不能分配给对象，最后是面向功能设计（也就是微服务的单一职责，减少协作）。相对地使用领域建模ddd</p><img src = "/img/javaee-1/2408ef7b9fd4e2b5b806064c327dc8bf.png" width = "400">  <p>各个模块之间采用RESTful API方式（严格保证单向通知，不能出现双向调用）或者使用消息通知机制（RabbitMQ……）来实现通讯</p><p><img src="/img/javaee-1/f774373bc6f00ce7212bcae3af0635ca.png">  </p><ul><li>集中配置：同一个服务的配置文件只放在一个集中的机器上面，其他相同服务机器只需要根据这个机器的application.yaml文件改配置。包括生产的配置文件里面都有密码，要把配置文件放在集中配置的机器上面，不能公开；以及定时刷新更换配置</li><li>客户透明：服务之间的调用要发现、上线，不能采用IP的方式，需要有一个透明的管理，只需要服务模块的名字就可以</li><li>负载均衡和容错：返回服务负载比较小的机器</li></ul><h2 id="服务发现-网关"><a href="#服务发现-网关" class="headerlink" title="服务发现 网关"></a>服务发现 网关</h2><p>集中配置Nacos：先去读spring cloud bootstrap 找到Nacos的地址，然后从上面选择application.yaml来更换本地配置<br><img src = '/img/javaee-1/bed377f1cc72522dbf951ea35abaf840.png' alt = '' width = "400"><br>客户端再把自己的方法注册到Nacos集群之后，同时也拉下了上面的所有的服务IP地址，作为自己的客户端缓存。同时还会有心跳机制客户端不断向Nacos发送心跳表明自己的服务是正常的，同时Nacos返回新的服务供客户端更新。客户端用最简单的轮询或者随机实现动态负载均衡，这也是在客户端做的。</p><p><img src="/img/javaee-1/d329f74e180430faf8e6fe1fc3beaeab.png"><br>微服务里面所有的接口都通过API网关来向外暴露，防止攻击，便于管理</p>]]></content>
    
    
    <categories>
      
      <category>技术笔记</category>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>java</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>书影音随记</title>
    <link href="/media-notes/"/>
    <url>/media-notes/</url>
    
    <content type="html"><![CDATA[<h1 id="喜欢的"><a href="#喜欢的" class="headerlink" title="喜欢的"></a>喜欢的</h1><h2 id="毒枭-season-1-3"><a href="#毒枭-season-1-3" class="headerlink" title="毒枭(season 1 - 3)"></a>毒枭(season 1 - 3)</h2><p><a href="">毒枭</a> 看这个剧也像是磕嗨了……前两天才看到哥伦比亚总统候选人被枪杀，开始觉得剧里面的情节很可能还是现实美化过的，在这样的国家里面选择去当一名警察真的是选择把命交给国家，触及到了利益毒贩就会开始报复虐杀警察。人物塑造得很立体，season2帕波离开自己的亲人，好像也是一个大肚子小胡子的普通人，走向没落。鬓角不断变白窥见这个毒品帝国掌权人的一点点衰退，和他之前凶狠随意杀掉成千上万的警察、平民又是很矛盾。  </p><p>帕波演员瓦格纳·马拉让我特别惊喜，几乎很少看到表演的痕迹，表情和眼神粗看可以说一个板子，但就是能在剧里面的情节上让人觉得他还在盘算、愤怒、蛰伏、骄傲……太丰富了，只有最后面对自己的父亲，他心里的话才说出来。就算他大势已去，躲在多年不见的父亲偏僻农场里，听着爸爸说“我对你感到羞耻，因为我觉得你是杀人犯”，情绪的波动和眼神，太绝了。最后还是把他所有的六千美金给了爸爸。总是让人联想这个巨头会在想什么……帕波的凶残，杀上万的平民和警察，又忠于自己的家庭。  </p><blockquote><p>原来瓦格纳还是这部剧的导演啊，太佩服了。  </p></blockquote><p>很有极致的细节下隐藏的魔幻现实主义，too strange to believe.<br><em>2025.6</em></p><h2 id="苦尽柑来遇见你"><a href="#苦尽柑来遇见你" class="headerlink" title="苦尽柑来遇见你"></a>苦尽柑来遇见你</h2><p><a href="https://movie.douban.com/subject/36053256/">苦尽柑来遇见你</a> 我在这部剧火了有段时间之后才开始看，现在评分 9.5。这部剧看起来不是那种可以让人很轻松地去观察，或者说代入进去，因为它展示的普通人困境总是多少让我想到自己或者身边人。也许是导演有意为之，大大小小的催泪桥段让我觉得真的很“苦”。看的时候我就在想，写出这么多真实的下意识的反应的编剧或者导演，他们是不是也把自己的经历投射到影片当中？因为实在有些隐秘和暗中的情感，放出来的一帧的人物的表情，都能让人在心里想：欸，我的爸妈当时也是这样……我也有过这样的情绪……  </p><p>这部大热的韩剧不可避免让人想起来《请回答 1988》，我觉得两个不一样的点很简单。《请回答》是笑着看下去，最后流泪；《苦尽柑来遇见你》是流着泪看，最后会留下笑容。慢慢地看下去，做好看见自己和父辈影子的准备，只需观察。</p><p><em>2025.5</em></p><h2 id="国境以南-太阳以西"><a href="#国境以南-太阳以西" class="headerlink" title="国境以南 太阳以西"></a>国境以南 太阳以西</h2><p><a href="https://book.douban.com/subject/1015452/">国境以南 太阳以西</a> 村上的书看过几本，写少年情爱的情愫思想都很类似。正好看到一段书里的话感觉贴切，放在这里。</p><div class="note note-info">                        <div class="note-content">              <p><em>我犯过几个错误，实际上那甚至连错误都不是。与其说是错误，或许莫如说是我自身与生俱来的倾向性东西。如此想着，我黯然神伤。</em></p>            </div>          </div>  <p> 大概就是这种感觉。</p><hr><p>（看完再评）: 小说后半段太棒了，村上写了那么多岛本和初君之间的故事，好像又是在写泉。岛本太飘渺了，又很神秘，倒是初君好像一直心事重重。最后结尾见到的是泉，心头一颤，还有消失的十万元信封和唱片，都像是初君自己的幻想。一旦向着太阳以西的方向走，就会得病，直到看见和岛本一样走路跛着脚的泉。 </p><p>发现自己村上的好几本书都看过了，《挪威的森林》《1Q84》《国境》。都是很像的留白疏离感。我喜欢他的短篇和中篇，长篇里喜欢《挪威》。</p><div class="note note-info">                        <div class="note-content">              <p><strong>“然而我无论如何也无法从厨房桌前站起，似乎所有气力都已从身上消失，就好像有人悄悄绕到我背后轻轻拔去我的体塞。我臂肘挂着桌面，双手捂脸。</strong><br><strong>黑暗中我想到落于海面的雨一一浩瀚无边的大海上无声无息地、不为任何人知晓地降落的雨。雨安安静静地叩击海面，鱼们甚至都浑然不觉。</strong><br><strong>我一直在想这样的大海，直到有人走来把手轻轻放在我的背上。”</strong> </p>            </div>          </div>  <p><em>2025.6</em></p><h2 id="遇见百分百的女孩"><a href="#遇见百分百的女孩" class="headerlink" title="遇见百分百的女孩"></a>遇见百分百的女孩</h2><p>村上的这部短篇小说集我只看了《百分百》这一篇，还是他擅长的留白的疏离感。挺喜欢的，原文放在<a href="/meet/">这里</a>吧。</p><h2 id="最后生还者-The-Last-of-Us"><a href="#最后生还者-The-Last-of-Us" class="headerlink" title="最后生还者(The Last of Us)"></a>最后生还者(The Last of Us)</h2><p><a href="">最后生还者</a> 之前看的是第一季，我还挺喜欢这种偏丧尸题材的，还有“乔伊”这样的养父形象和 Ellie 搭配就很让人期待。前面的雪地小镇围攻确实很好看，可惜乔伊死了，后面的复仇看得有点乱，确实没有第一季精彩。看完第二季之后又去重温第一季了……</p><h1 id="杂书"><a href="#杂书" class="headerlink" title="杂书"></a>杂书</h1><h2 id="看见"><a href="#看见" class="headerlink" title="看见"></a>看见</h2><div class="note note-warning">                        <div class="note-content">              <p><strong>看见自有万钧之力</strong>。</p>            </div>          </div><p>这本书最近开始在大陆软封禁，恰好也在 Youtube 上关注了柴静，去图书馆借了这本书来看一看。略有失望，没有我想象中“禁书”的感觉，都是柴静作为一名新闻工作者的记录（或者说更像是记者成长的日记，和工整的报道不一样，读起来还有点像李娟的文风）。被封禁可能是作者记录的新闻和当前的宏大叙事下的政治宣传调子不一致，我看到一个留言说是“《看见》这本书看见了它不该看见的东西”，一时讽刺。</p><p>每每谈论到政治或者敏感的新闻话题，我们总是没有办法“看见”这些新闻最真实的样子。可能是新闻本身的报道就受到了管制，或者大多数也是营销号的让人犯恶心的假消息，看见变成一件很难的事。看看以前的新闻人是怎么对待把事实呈现出来的方式，对比现在的“新闻”，就是在网上看到“学新闻学的”讥讽的话也觉得有三分对。</p><p>我之前是没有很想去了解事实的真相、政治之下。一方面墙内在敏感话题上很少有客观公正的展示，经过粉饰、包装、美化或是成梗、嬉笑都没有自己“看见”来得真实，特别是口罩时代后似乎如履薄冰；另一方面觉得是与我无关。柴静在 Youtube 某一个视频上说“（大意）更好地了解历史，是为了让它再次出现在我们身边的时候，我们至少能够认出它”。希望她可以一直做下去。<br><em>2025.6</em></p><h1 id="看不下去的"><a href="#看不下去的" class="headerlink" title="看不下去的"></a>看不下去的</h1><h2 id="半泽直树"><a href="#半泽直树" class="headerlink" title="半泽直树"></a>半泽直树</h2><p><a href="https://book.douban.com/subject/34428990/">半泽直树</a> 非影视剧，阅读之前不知道有很火的电视剧。我在书架上看到一排的系列书，开始翻两页说到半泽的职业梦想以为是很青春热血的小说，加之对日本的泡沫经济时期感兴趣，猜测后续的发展可能是职场上的成长啊之类的。没想到是个复仇计划，和银行里黑暗的不公平的势力作斗争。</p><p>据说电视剧更好看，但是本人对这种题材并不十分感兴趣，读起来也像是流水账，推理悬疑效果比较弱，人物塑造比较喜欢被外派的银行职员，相比半泽这种天生反抗有光环的，更像是一个普通人面对时代浪潮不堪一击还要苦苦支撑家庭生活，即使外调也在振作，这样更打动人些吧。<br>看不下去这题材。<br><em>2025.4-5</em></p><h2 id="河边的错误"><a href="#河边的错误" class="headerlink" title="河边的错误"></a>河边的错误</h2><p><a href="https://book.douban.com/subject/36441149/">河边的错误</a> 老实说第一篇《河边的错误》我还是带着兴趣慢慢看下去了，只是后面看的越来越觉得怪异，一些细微的人物反应或情绪让人很费解，情节也好象是自顾自地展开。我还是比较喜欢稍有逻辑性的情节，所以看完了也不知道说了一个什么故事后还是蛮失落的（没有看电影，似乎是正确的）。余华老师自己说这是一部记录他“曾经有过的疯狂”，我能感受到那种疯狂、血淋淋，和网上大喊的“阴暗里爬行”是两种东西。自己不太喜欢，后面其他的故事也没看完。<br><em>2025.5</em></p>]]></content>
    
    
    <categories>
      
      <category>WLB</category>
      
      <category>随记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>书影音</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>凤凰架构-远程服务、事务1</title>
    <link href="/%E5%87%A4%E5%87%B0%E6%9E%B6%E6%9E%84-%E8%BF%9C%E7%A8%8B%E6%9C%8D%E5%8A%A1%E3%80%81%E4%BA%8B%E5%8A%A11/"/>
    <url>/%E5%87%A4%E5%87%B0%E6%9E%B6%E6%9E%84-%E8%BF%9C%E7%A8%8B%E6%9C%8D%E5%8A%A1%E3%80%81%E4%BA%8B%E5%8A%A11/</url>
    
    <content type="html"><![CDATA[<h1 id="远程服务"><a href="#远程服务" class="headerlink" title="远程服务"></a>远程服务</h1><h2 id="RPC-远程调用"><a href="#RPC-远程调用" class="headerlink" title="RPC 远程调用"></a>RPC 远程调用</h2><p>rpc 本质是 <strong><em>将本地方法调用的思路迁移到远程方法，同时为了解决异构机器之间的问题，需要设计不同的协议或者接口</em></strong>。</p><h2 id="A1-：RPC-是怎么被提起的？它遇到什么困难？"><a href="#A1-：RPC-是怎么被提起的？它遇到什么困难？" class="headerlink" title="A1 ：RPC 是怎么被提起的？它遇到什么困难？"></a>A1 ：RPC 是怎么被提起的？它遇到什么困难？</h2><p>远程服务将计算机工作从单机扩展到网络，从本地延展到远程。不可避免地，如何在 caller, callee 不是同一进程空间的条件下仍然能够像普通调用函数一样执行。如果是本机，可以借助共享内存，让多个进程同时访问一个地址空间；跨机器的话，利用 socket 让函数参数经过网络协议栈，打包解包，计算校验和，维护序列号和应答号。<br>设计之初，rpc 还有很多的问题亟待解决：</p><ul><li>跨机器之间的网络通信成本（rpc 不等于 ipc）</li><li>进程间交换数据</li><li>机器之间支持的不同指令集、不同编程语言<br>总的来说，需要解决三个基本问题：</li></ul><ol><li>如何表示数据（序列化与反序列化，跨语言跨机器之间用约定好的中立数据格式传输：JSON XML）</li><li>如何传递数据（应用层协议交换信息协议，解决类似超时、异常、安全、认证、事务等需求）</li><li>如何表示方法（统一的跨语言标准，能够找到准确的 callee）</li></ol><div class="note note-tip">                        <div class="note-content">              <p><strong>总结</strong><br>TODO</p>            </div>          </div><h2 id="REST-设计"><a href="#REST-设计" class="headerlink" title="REST 设计"></a>REST 设计</h2><p>REST 风格是一种面向资源编程思想，让开发者专注于抽象资源对应到 HTTP 协议的操作中，强调统一接口和面向资源而非服务或者动作，具有很强的可读性和统一性，但性能和处理逻辑上有欠缺。</p><h2 id="A1-：如何确认一个系统是不是-REST-风格？"><a href="#A1-：如何确认一个系统是不是-REST-风格？" class="headerlink" title="A1 ：如何确认一个系统是不是 REST 风格？"></a>A1 ：如何确认一个系统是不是 REST 风格？</h2><p>REST 风格有如下特点：</p><ul><li>客户端无状态原则：“REST 希望服务器不要去负责维护状态，每一次从客户端发送的请求中，应包括所有的必要的上下文信息，会话信息也由客户端负责保存维护，服务端依据客户端传递的状态来执行业务处理逻辑，驱动整个应用的状态变迁。” 客户端把历史的状态信息隐藏到每次的请求（头部？）中，传递给服务端。大型的系统要求传递的状态可能是巨大的，所以服务端本地的状态缓存也有必要.</li><li>与 Http 1.1 协议的的高度绑定：<strong>这两个的核心思想都是 “ 面向资源”</strong> ，设计了 GET POST PUT DELETE PUTCH 这样对资源的操作行为。<strong><em>把服务中的实体抽象出来成为一种资源，对资源进行设计统一的接口</em></strong></li><li>建议：对资源的操作通过 ID 进行；通过返回消息的超文本驱动状态的转变，指向下一个操作的接口</li><li>……</li></ul><h2 id="A2：REST-的争议？"><a href="#A2：REST-的争议？" class="headerlink" title="A2：REST 的争议？"></a>A2：REST 的争议？</h2><ul><li><strong>资源粒度大小</strong>：REST 缺乏对资源进行“部分”和“批量”的处理能力：限制了资源的大小，每次请求返回的是资源对象的所有属性信息，可能并不需要这么多，然而 http 协议没有对请求资源的结构化描述，所以只能在 endpoint 后面加上参数或者表单上加上字段限制；</li><li><strong>批量获取资源的能力</strong>：批量则是指如果对于一个重复性的 request 请求，为了避免大量的传输请求，服务端要设计一个批量的 ”资源“ （可能已经脱离了面向一个实体类型资源的讨论，而是 <strong><em>抽象一种批量处理的动作，称之为”事务“ 的资源</em></strong> ）来对这个批量的资源请求。</li></ul><h2 id="A3：对比一下-rpc，rest？"><a href="#A3：对比一下-rpc，rest？" class="headerlink" title="A3：对比一下 rpc，rest？"></a>A3：对比一下 rpc，rest？</h2><p>可以看到 rest 风格主要是<strong>关注资源怎么被高效地抽象出来，能够覆盖更多的操作，也就是更加的 restful 风格</strong>。它把设计的统一接口固定了，绑定的协议也固定了，HTTP 规范是优点也是缺点。开发者能够不用关心更多的细节、接口的繁琐；而 rpc 提供了更自由、更灵活的配置，当然它的代价也更大。</p><h1 id="事务处理"><a href="#事务处理" class="headerlink" title="事务处理"></a>事务处理</h1><h2 id="本地事务"><a href="#本地事务" class="headerlink" title="本地事务"></a>本地事务</h2><h2 id="实现原子性和持久性"><a href="#实现原子性和持久性" class="headerlink" title="实现原子性和持久性"></a>实现原子性和持久性</h2><p>原子性指一个事务要么都成功，要么都失败，不存在中间状态；持久性指事务生效后，不会因为其他的原因导致修改失效。然而这和实际的磁盘读写和不符的，磁盘读写可能存在只写了一半出现崩溃的情况。所以我们需要辅助机制来帮助实现这两个目标。<br>“基于语义的恢复与隔离算法”ARIES 提出了日志辅助的方法，它的核心思想是，对每一个事务的读写都要经过日志，状态包括 start, commit, endlog 状态，<strong><em>读写数据要在事务 commit 之后才能进行原子性的读写</em></strong>，持久化则可以扫描日志异步持久化到磁盘以提高 IO。</p><p>崩溃时间点可以分为：</p><ul><li><strong>未提交事务，写入后崩溃</strong>：程序还没修改完三个数据，但数据库已经将其中一个或两个数据的变动写入磁盘，此时出现崩溃，一旦重启之后，需要回滚撤销已修改的数据</li><li><strong>已提交事务，写入前崩溃</strong>：程序已经修改完三个数据，但数据库还未将全部三个数据的变动都写入到磁盘，此时出现崩溃，一旦重启之后，根据日志继续把所有数据的改动写入磁盘</li></ul><p>Write-Ahead Logging 先将何时写入变动数据，按照事务提交时点为界，划分为 FORCE 和 STEAL 两类情况。</p><ul><li><strong>FORCE</strong>：当事务提交后，要求变动数据必须同时完成写入则称为 FORCE，如果不强制变动数据必须同时完成写入则称为 NO-FORCE。现实中绝大多数数据库采用的都是 NO-FORCE 策略，因为只要有了日志，变动数据随时可以持久化，从优化磁盘 I&#x2F;O 性能考虑，没有必要强制数据写入立即进行。</li><li><strong>STEAL</strong>：在事务提交前，允许变动数据提前写入则称为 STEAL，不允许则称为 NO-STEAL。从优化磁盘 I&#x2F;O 性能考虑，允许数据提前写入，有利于利用空闲 I&#x2F;O 资源，也有利于节省数据库缓存区的内存。</li></ul><div class="note note-info">                        <div class="note-content">              <p><strong>例子分析</strong></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs sql">[<span class="hljs-number">100</span>] CHECKPOINT <span class="hljs-keyword">start</span><br>[<span class="hljs-number">101</span>] T1 <span class="hljs-keyword">START</span><br>[<span class="hljs-number">102</span>] T1 <span class="hljs-keyword">UPDATE</span> A: <span class="hljs-number">100</span> <span class="hljs-operator">-</span><span class="hljs-operator">&gt;</span> <span class="hljs-number">70</span>   <span class="hljs-comment">-- undo = 100, redo = 70</span><br>[<span class="hljs-number">103</span>] T1 <span class="hljs-keyword">UPDATE</span> B: <span class="hljs-number">50</span> <span class="hljs-operator">-</span><span class="hljs-operator">&gt;</span> <span class="hljs-number">80</span>    <span class="hljs-comment">-- undo = 50,  redo = 80</span><br>[<span class="hljs-number">104</span>] T1 <span class="hljs-keyword">COMMIT</span><br><span class="hljs-comment">--- crash 发生在这里 ---</span><br>[<span class="hljs-number">105</span>] T1 <span class="hljs-keyword">END</span>                   <span class="hljs-comment">-- ❌ 未写入</span><br>[<span class="hljs-number">106</span>] T2 <span class="hljs-keyword">START</span><br>[<span class="hljs-number">107</span>] T2 <span class="hljs-keyword">UPDATE</span> A: <span class="hljs-number">70</span> <span class="hljs-operator">-</span><span class="hljs-operator">&gt;</span> <span class="hljs-number">50</span>    <span class="hljs-comment">-- undo = 70, redo = 50</span><br></code></pre></td></tr></table></figure><p>从上一次的 checkpoint 开始扫描，如果发现 commit 但未 end 日志，加入到 redo 日志，异步完成重做；如果发现 start 但是未 commit，加入到 undo log 撤销操作。</p>            </div>          </div><p>WAL 崩溃恢复进行下面三个阶段的操作：</p><ul><li><strong>分析阶段</strong>（Analysis）：该阶段从最后一次检查点（Checkpoint，可理解为在这个点之前所有应该持久化的变动都已安全落盘）开始扫描日志，找出所有没有 End Record 的事务，组成待恢复的事务集合，这个集合至少会包括 Transaction Table 和 Dirty Page Table 两个组成部分。</li><li><strong>重做阶段</strong>（Redo）：该阶段依据分析阶段中产生的待恢复的事务集合来重演历史（Repeat History），具体操作为：找出所有包含 Commit Record 的日志，将这些日志修改的数据写入磁盘，写入完成后在日志中增加一条 End Record，然后移除出待恢复事务集合。</li><li><strong>回滚阶段</strong>（Undo）：该阶段处理经过分析、重做阶段后剩余的恢复事务集合，此时剩下的都是需要回滚的事务，它们被称为 Loser，根据 Undo Log 中的信息，将已经提前写入磁盘的信息重新改写回去，以达到回滚这些 Loser 事务的目的。</li></ul><h2 id="实现隔离性"><a href="#实现隔离性" class="headerlink" title="实现隔离性"></a>实现隔离性</h2><p>隔离性是要求不同事务的并发读写互相不干扰，一般使用加锁的方式实现隔离。</p><ul><li>写锁又称排他锁，如果数据有加写锁，就只有持有写锁的事务才能对数据进行写入操作，数据加持着写锁时，其他事务不能写入数据，也不能施加读锁</li><li>读锁，多个事务可以对同一个数据添加多个读锁，数据被加上读锁后就不能再被加上写锁，所以其他事务不能对该数据进行写入，但仍然可以读取。</li><li>范围锁，对一个范围加排他锁，<strong>不能在这个范围内修改、新增、删除数据</strong></li></ul><p>隔离分为 可串行化、可重复读、读已提交、读未提交 三个主要的级别，差别在于加锁的范围和时机不同，达到了不同的效果。本质还是依靠上面的三种类型的锁实现隔离。在思考下面几种隔离级别带来的问题时，要特别注意对象是不同的事务，在内部事务没有 commit 的情况下，锁是否还生效导致其他的事务会不会读&#x2F;写同一范围的数据，<em>而一个事务包含的 sql 语句是很多的</em>。</p><ul><li>可串行化：并发事务的读写可以等效为某一种串行的读写请求。对事务所有读、写的数据全都加上读锁、写锁和范围锁</li><li>可重复读：对事务所涉及的数据加读锁和写锁，且一直持有至事务结束，但不再加范围锁。带来的问题是幻读，如果在两次范围读之间插入写，那么<strong>范围查询是不同的集合</strong>。MySQL 的引擎 Innodb 默认的隔离级别是可重复读，在只读事务的条件下可以避免幻读</li><li>读已提交：对事务涉及的数据加的写锁会一直持续到事务结束，但<strong>加的读锁在查询操作完成后就马上会释放</strong>。带来了不可重复读问题，缺乏贯穿整个事务周期的读锁，无法禁止读取过的数据发生变化，在下一次读取可能会读到其他事务写的数据。</li><li>读未提交：对事务涉及的数据只加写锁，会一直持续到事务结束，但<strong>完全不加读锁</strong>。意味着事务写后回滚，可能会有别的事务在其中读到了回滚前的数据，造成脏读。</li></ul>]]></content>
    
    
    <categories>
      
      <category>技术笔记</category>
      
      <category>系统设计</category>
      
      <category>凤凰架构</category>
      
    </categories>
    
    
    <tags>
      
      <tag>架构</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Java并发编程</title>
    <link href="/JAVA%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/"/>
    <url>/JAVA%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/</url>
    
    <content type="html"><![CDATA[<h1 id="多线程初探：线程产生、同步、原理"><a href="#多线程初探：线程产生、同步、原理" class="headerlink" title="多线程初探：线程产生、同步、原理"></a>多线程初探：线程产生、同步、原理</h1><p>提交到线程池的可以是：</p><ul><li><strong>实现了  <code>Runnable</code>  接口的任务</strong></li><li><strong>实现了  <code>Callable</code>  接口的任务</strong></li><li><strong><code>FutureTask</code>  类型的任务</strong><br>而 FutureTask 既可以作为 Runnable 被线程执行，又可以作为 Future 得到 Callable 的返回值。用于异步计算？因为可以之后再 get() 线程的结果.</li></ul><p><code>object.wait()</code> 必须在 <code>synchronized</code> 方法中使用，<strong>会自动放弃锁</strong>，直到别的线程 <code>notify</code> 才能进入 <code>RUNNABLE</code> 状态，等待线程调度。<br><code>object.sleep()</code> 并不会放弃锁<br><img src="/img/java%E5%B9%B6%E5%8F%91/1be332e11035cabc8d148c96ce1e3ebb.png"><br>线程组的数据结构</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadGroup</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Thread</span>.UncaughtExceptionHandler &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ThreadGroup parent; <span class="hljs-comment">// 父亲ThreadGroup</span><br>    String name; <span class="hljs-comment">// ThreadGroup 的名称</span><br>    <span class="hljs-type">int</span> maxPriority; <span class="hljs-comment">// 最大优先级</span><br>    <span class="hljs-type">boolean</span> destroyed; <span class="hljs-comment">// 是否被销毁</span><br>    <span class="hljs-type">boolean</span> daemon; <span class="hljs-comment">// 是否守护线程</span><br>    <span class="hljs-type">boolean</span> vmAllowSuspension; <span class="hljs-comment">// 是否可以中断</span><br><br>    <span class="hljs-type">int</span> <span class="hljs-variable">nUnstartedThreads</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; <span class="hljs-comment">// 还未启动的线程</span><br>    <span class="hljs-type">int</span> nthreads; <span class="hljs-comment">// ThreadGroup中线程数目</span><br>    Thread threads[]; <span class="hljs-comment">// ThreadGroup中的线程</span><br><br>    <span class="hljs-type">int</span> ngroups; <span class="hljs-comment">// 线程组数目</span><br>    ThreadGroup groups[]; <span class="hljs-comment">// 线程组数组</span><br>&#125;<br></code></pre></td></tr></table></figure><ul><li>一个进程创建的所有线程，都是共享一个内存空间的；cpu 切换任务时机是在指令级，不在语言级别。同时还需要考虑编译器优化，指令重排对多线程访问有序性的影响。</li><li>多线程编程出现并发错误的源头在于：<strong>可见性问题</strong>（cpu 缓存是的不同线程读取到了未更新的数据）、<strong>有序性问题</strong>（编译器优化使得在不同时间片切换，线程访问错误）</li><li>Happens-Before 规则：<strong>前面一个操作的结果对后续操作是可见的</strong>（这里指的都是对于共享变量的操作，是可见的）</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 有趣的双重检查创建单例代码</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Singleton</span> &#123;<br>  <span class="hljs-keyword">static</span> <span class="hljs-keyword">volatile</span> Singleton instance;  <span class="hljs-comment">// 加上volatile 禁止指令重排序才能创建合理的单例</span><br><br>  <span class="hljs-keyword">static</span> Singleton <span class="hljs-title function_">getInstance</span><span class="hljs-params">()</span>&#123;<br>    <span class="hljs-keyword">if</span> (instance == <span class="hljs-literal">null</span>) &#123;<br>      <span class="hljs-keyword">synchronized</span>(Singleton.class) &#123;<br>        <span class="hljs-keyword">if</span> (instance == <span class="hljs-literal">null</span>)<br>          instance = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Singleton</span>();<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> instance;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>😭 指令重排（有序性问题）可能会导致顺序 分配内存（memory &#x3D; allocate()）、将 instance 指向这块内存（instance &#x3D; memory）、 初始化对象（调用构造方法）。这导致其他线程可能访问了未初始化完全的变量。Java 使用了 volatile：禁止指令重排序 解决此问题。所以这个例子目前是可行的。</p><div class="note note-info">                        <div class="note-content">              <p>volatile 关键字怎么解决并发中的经典问题<br><strong>禁止线程用自己的缓存，使得线程对变量的修改可以让所有的线程可见！和禁止指令重排，解决有序性问题</strong></p>            </div>          </div>  <p>🤔JVM 会等待工作线程全部结束之后才会停止主线程服务?<br>是的，JVM 确实会等待，但并不意味着主线程 main 也会等待，这就是我们需要 join 的原因。join 会让主线程阻塞从而等待子线程优先于主线程结束，执行逻辑正确；否则主线程可能中途结束执行到了后面的逻辑。</p><p>同步原语 synchronized：</p><ul><li>static synchronized function（）会在类级别的上加锁，整个 JVM 只有一把锁。</li><li>🤔 如何锁住其他对象呢？传递 object 对象作为参数，锁住 object。这也意味每次要锁住都必须传入相同的 object，会有难度</li></ul><p>线程池：<br><img src="/img/java%E5%B9%B6%E5%8F%91/3719561120dd8ae5a63442180fd55b47.png"></p><ul><li>关注 corePoolSize，线程池保证这部分线程始终能够接受任务，不会轻易回收。而 corePoolSize &lt;&#x3D; 线程数量&lt;&#x3D; MaxiumPoolSize 这部分非核心线程会在任务完成之后一段时间内被回收。</li><li>如果 MaxiumPoolSize 设置非常大，或者 workQueue 是无界队列，拒绝策略将不会生效，永远堆不满。</li><li>❌ 缓存（无线制队列和任务）、简单（只能一个）、定长线程池（core&#x3D;maxium）都太极端，自定义的可以。</li></ul><h1 id="并发同步：锁、线程本地"><a href="#并发同步：锁、线程本地" class="headerlink" title="并发同步：锁、线程本地"></a>并发同步：锁、线程本地</h1><p>⭐Lock 接口：<br>✅ 可重入锁：其本身含义是指 <strong>同一个线程可以多次获取已经持有的锁</strong><br>ReentrantLock 有 lock() 、trylock() 方法，其中 trylock() 有返回值 boolean，true 时获得锁进入，立即返回。等待时间内获取到锁，trylock( long time )是一段时间的自旋锁。❓ 是不是可以缓解死锁的出现呢<br>注意，finally 语句中都要将锁释放，lock.unlock( )。<br>公平性上，默认的都是非公平锁，意味着线程不一定按照到达锁的时间按顺序排队获得锁，而是按照策略选择一个队列中的线程执行。</p><p>✅ 可重入读写锁：读锁是共享锁，多个线程可同时进入临界区；写锁是独占锁。<br>他们在公平性上，对于同一份资源，如果读锁在队列的头部，那么新的读线程可能会插队；反之，如果是写锁，那么读线程会排到队列的末尾。提高性能之举。<br><img src="/img/java%E5%B9%B6%E5%8F%91/21a0fecd208e7a0cba8bfa5f7504e99b.png"><br>❓ 关于 reentrantlock 和 synchronized 关键字的对比</p><table><thead><tr><th>对比维度</th><th><code>synchronized</code></th><th><code>ReentrantLock</code></th></tr></thead><tbody><tr><td>所在包</td><td><code>java</code> 关键字，属于 JVM 语法级别</td><td><code>java.util.concurrent.locks</code> 中的类</td></tr><tr><td>加锁&#x2F;释放方式</td><td>自动加锁、自动释放（异常也会自动释放）</td><td>需要手动加锁 <code>lock()</code> 和释放 <code>unlock()</code>，要写在 finally</td></tr><tr><td>可重入性</td><td>✅ 是</td><td>✅ 是</td></tr><tr><td>公平锁支持</td><td>❌ 不支持，默认非公平</td><td>✅ 可设置为公平锁（先来先得）</td></tr><tr><td>可中断锁</td><td>❌ 不支持</td><td>✅ 支持 <code>lockInterruptibly()</code></td></tr><tr><td>尝试获取锁（不阻塞线程）</td><td>❌ 不支持</td><td>✅ 支持 <code>tryLock()</code></td></tr><tr><td>是否支持条件变量</td><td>❌ 不支持（只能用 <code>wait()/notify()</code>）</td><td>✅ 支持 <code>Condition</code> 类，可实现多个等待队列</td></tr><tr><td>性能（JDK1.6 之后）</td><td>已优化，引入<strong>偏向锁&#x2F;轻量级锁</strong>，性能很好</td><td>高并发下性能优秀，功能灵活</td></tr><tr><td>可见性与原子性</td><td>✅ 内置保证（由 JVM 实现）</td><td>✅ 依赖底层实现，效果相同</td></tr><tr><td>锁的粒度</td><td>隐式对象级别</td><td>灵活控制（可锁方法块、不同对象）</td></tr><tr><td>使用难度</td><td>✅ 简单，关键字一写就好</td><td>⚠️ 稍复杂，需配合 try-finally 写法（finally 里面写上 unlock，防止并发错误）</td></tr><tr><td>reentrantlock 更适合可中断、限时等待、多条件变量的场景，当然 synchronized 关键字已经优化的很好了</td><td></td><td></td></tr></tbody></table><h1 id="并发流程控制"><a href="#并发流程控制" class="headerlink" title="并发流程控制"></a>并发流程控制</h1><p>❓<strong>多个线程之间存在流程控制怎么办，考虑并发顺序和流程，而非并发安全问题！</strong></p><table><thead><tr><th>特性</th><th>CountDownLatch</th><th>CyclicBarrier</th><th>Semaphore</th></tr></thead><tbody><tr><td>等待机制</td><td>等待 N 次 <code>countDown()</code></td><td>等待 N 个线程 <code>await()</code></td><td>获取 N 个许可才可进入</td></tr><tr><td>可否重用</td><td>❌ 不可重用</td><td>✅ 可重用</td><td>✅ 可重用</td></tr><tr><td>回调功能（触发动作）</td><td>❌ 没有</td><td>✅ 有（barrier action）</td><td>❌ 没有</td></tr><tr><td>应用场景</td><td>等待其他线程完成（等所有学生到了再开班会，只关心人数是否齐全）</td><td>线程“集结点”，到达屏障点之后再统一执行任务。可以一环扣着一环</td><td>控制并发访问，允许最多多少个线程同时进入临界区</td></tr><tr><td>控制的是谁</td><td>主线程等待子线程</td><td>所有线程互相等待</td><td>控制访问资源的线程数量</td></tr><tr><td>实现原理</td><td>AQS 共享锁</td><td>AQS + 屏障机制</td><td>AQS 共享锁 + 许可证信号量</td></tr><tr><td>用法</td><td>await( ) 和 countdown( )配合， await 用来等待</td><td>await( )阻塞标记单个任务执行完毕，计数器-1 直到为 0 执行回调函数</td><td></td></tr></tbody></table><p>✅CountDownLatch 适合协调多个线程完成某项任务（等待一组任务完成后再执行）：<br>使用 await( ) countdown( ) api 用于同步。当 count 减少到 0 之后 await 阻塞。主线程调用 <code>await()</code> 等待其他线程调用 <code>countDown()</code></p><p>✅CyclicBarrier 适合多线程并行执行后统一汇总（后面还有统一的任务）：</p><p>❌threadlocal？？</p><ol><li>线程隔离作用：每个线程都有着自己的资源，避免线程之间的共享导致并发不安全。另外实现类：threadlocal 的 getter、setter、remove 方法</li><li>上下文特性，减少线程内同一个实例反复传递参数繁琐：<br><img src="/img/java%E5%B9%B6%E5%8F%91/1ae14f5a19438c6dc6affa445446cd64.png"><br>threadlocal 内存泄漏问题？</li></ol><h1 id="CAS-原子操作"><a href="#CAS-原子操作" class="headerlink" title="CAS 原子操作"></a>CAS 原子操作</h1><p>CAS 基于乐观锁的思想实现，底层是硬件上的 compare and set，解决 “<strong>一个变量在多线程下原子修改</strong>” 问题。 涉及到 current value、expect value、set value，如果当前值和期望值相同，那么就会设置新的值。这个过程会保证自旋，设置自旋失败次数上限防止长时间占用锁。和悲观锁 synchroized reentrantLock 不同的是，cas 是多线程同时进入的，他们各自拿到内存的值，get compare if correct then set，而悲观锁确保在同一时间只能有一个线程拿到锁（非读锁、信号量）</p><p>❓ 是不是自旋一定会带来并发性能下降？不一定，如果临界区很小，线程不是很多，频繁的切换线程浪费更多。</p><table><thead><tr><th>类型</th><th>具体类</th><th>说明</th></tr></thead><tbody><tr><td>Atomic* 基本类型原子类</td><td>AtomicInteger、AtomicLong、AtomicBoolean</td><td>基本类型的原子自增操作</td></tr><tr><td>Atomic*Array 数组类型原子类</td><td>AtomicIntegerArray、AtomicLongArray、AtomicReferenceArray</td><td></td></tr><tr><td>Atomic*Reference 引用类型原子类</td><td>AtomicReference（只能保证引用对象原子更新，但是内部字段不保证）、AtomicStampedReference、AtomicMarkableReference</td><td>不替换整个对象的条件下，对字段进行原子更新。字段必须是 volatile，保证内存可见</td></tr><tr><td>Atomic*FieldUpdater 升级类型原子类</td><td>AtomicIntegerfieldupdater、AtomicLongFieldUpdater、AtomicReferenceFieldUpdater</td><td></td></tr><tr><td>Adder 累加器</td><td>LongAdder、DoubleAdder</td><td>使用分段锁，维护 Cell 数组中每个 cell 增加的值最终累加</td></tr><tr><td>Accumulator 积累器</td><td>LongAccumulator、DoubleAccumulator</td><td></td></tr></tbody></table><div class="note note-info">                        <div class="note-content">              <p>分段&#x2F;分区来减少锁冲突，一种将资源拆成多段，每段各自加锁的并发策略。LongAdder 底层维护 cell 数组（根据当前的线程数确定数组的大小），每个线程只在各自的 cell 上累加，不会出现冲突。也是分治思想</p>            </div>          </div><p><a href="https://learn.lianglianglee.com/%e4%b8%93%e6%a0%8f/Java%20%e5%b9%b6%e5%8f%91%e7%bc%96%e7%a8%8b%2078%20%e8%ae%b2-%e5%ae%8c/39%20%e5%8e%9f%e5%ad%90%e7%b1%bb%e6%98%af%e5%a6%82%e4%bd%95%e5%88%a9%e7%94%a8%20CAS%20%e4%bf%9d%e8%af%81%e7%ba%bf%e7%a8%8b%e5%ae%89%e5%85%a8%e7%9a%84%ef%bc%9f.md">atomic API 接口参考</a></p><h1 id="并发容器"><a href="#并发容器" class="headerlink" title="并发容器"></a>并发容器</h1>]]></content>
    
    
    <categories>
      
      <category>技术笔记</category>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>并发</tag>
      
      <tag>java</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Raft</title>
    <link href="/raft/"/>
    <url>/raft/</url>
    
    <content type="html"><![CDATA[<h2 id="论文笔记"><a href="#论文笔记" class="headerlink" title="论文笔记"></a>论文笔记</h2><h3 id="前置知识和概念："><a href="#前置知识和概念：" class="headerlink" title="前置知识和概念："></a>前置知识和概念：</h3><ul><li>共识算法常被用来确保每一个节点上的状态机一定都会按相同的顺序执行相同的命令， 并且最终会处于相同的状态（复制状态机模型）。它允许其中的节点出现故障后，整个系统仍然能保持运作，也就是高可用特性。具体来说，<strong>共识算法解决数据存储下的一致性问题，提供容错机制。</strong><blockquote><p>这里不得不提到复制状态机：多个节点在相同的初始状态下，执行相同的输入，那么最后的状态一定是相同的。这就解决了副本之间的一致性问题。在 Raft 算法中，leader 通过发送 log entry 请求到所有的 follower 节点，节点按照日志记录依次执行，那么最后的状态就是一致的。</p></blockquote></li><li>节点的状态只会有 follower，candidate，leader。如果节点之间没有收到 leader 的心跳，那么就会进入 candidate 状态，向所有节点并发发送请求包和响应请求。经过一次或多次的选举，选出 leader。</li><li>时间按照任期进行划分，每次开启新的任期，term+1</li><li>通信 rpc 类型包括：请求投票（投票选举 leader）和追加条目（由 leader 发起，用来追加日志条目和心跳机制）</li><li>还是论文当中的核心用几个问题来拆分，分别是以下问题：</li></ul><h3 id="A1：Raft-算法是如何避免脑裂问题的？"><a href="#A1：Raft-算法是如何避免脑裂问题的？" class="headerlink" title="A1：Raft 算法是如何避免脑裂问题的？"></a>A1：Raft 算法是如何避免脑裂问题的？</h3><p>从一个数学的角度，过半票决是怎么解决脑裂的？总结来看，脑裂问题主要需要解决单一性、一致性问题。</p><ul><li>单一性：为了保证不同网络分区总共也只能有一台主服务器，那么就要求服务器的数量是奇数的，保证分区服务器数量不对称，从而达到要求：<strong>在任何时候为了完成任何操作，你必须凑够过半的服务器来批准相应的操作</strong> 。这样保证过半的前提下，奇数服务器确保一个 leader</li><li>一致性：这更有趣，以选举为例，每一个操作对应的过半服务器，必然至少包含一个服务器存在于上一个操作的过半服务器中。也就是说，任意两组过半服务器，至少有一个服务器是重叠的。新 leader 依靠这个至少一台存在于旧 leader 的服务器，就能够知道旧 Leader 使用的任期号（term number）和任何旧 Leader 提交的操作。这是保证一致性的关键。</li></ul><h3 id="A2：Raft-作为一个共识算法，怎么和应用层接入？"><a href="#A2：Raft-作为一个共识算法，怎么和应用层接入？" class="headerlink" title="A2：Raft 作为一个共识算法，怎么和应用层接入？"></a>A2：Raft 作为一个共识算法，怎么和应用层接入？</h3><p>Raft 集群当中主要有两个接口一个是 key-value 接口，另一个是函数调用 Start 接口。key-value 层用来转发客户端请求的接口，Start 函数主要执行日志复制的过程。需要注意的是，key-value 接口向 Raft 层发送请求的时候，并不是同步的，客户端可以不用等待一个请求被确认 commit 之后在发送下一个请求，大量的请求同时发送是常见的。直到 Start 函数执行完毕后，Raft 层通过 applyCh 通道确认 key-value 已经成功执行，可以向客户端返回了。<br><img src="/img/gfs/f60d24f78d7223a24d832118e73983a3.png"></p><h3 id="A3：raft-集群当中的-commit、取消、和过半哲学？"><a href="#A3：raft-集群当中的-commit、取消、和过半哲学？" class="headerlink" title="A3：raft 集群当中的 commit、取消、和过半哲学？"></a>A3：raft 集群当中的 commit、取消、和过半哲学？</h3><p>在 A1 问题中，为了避免脑裂解释了过半是怎么保证一致性和单一性的。同时还有一些别的</p><h3 id="A4：Raft-是怎么解决多副本之间协调一致的问题？"><a href="#A4：Raft-是怎么解决多副本之间协调一致的问题？" class="headerlink" title="A4：Raft 是怎么解决多副本之间协调一致的问题？"></a>A4：Raft 是怎么解决多副本之间协调一致的问题？</h3><p><strong>采用 leader 选举的方法，有多个节点当中的 leader 决定 client 数据写入其他节点的顺序（类似于上面的 GFS 的主 primary ，但不同的是 GFS 是 master 指定的，而 Raft 是节点之间自主选举产生，依靠 RPC 发送日志条目）。</strong> follower 没有接收到心跳后，进入 candidate 状态，首先会把自己的任期 term+1，其次给自己投票，然后并发向所有的节点发送投票请求 RequestVote RPC。节点接收到投票请求，会给出一个响应，如果请求的 term &gt;&#x3D; 自己的 term，根据 ”先来先服务“ 的原则投出票。如此执行，直到有节点最先得到超过半数票，立即切换状态成为 leader，向所有其余节点发送心跳包，而其它节点收到心跳之后状态从 candidate 转换成 follower。至此，新的 leader 产生。<br>❓ 那么这样是不是会导致很多节点同时选举，票数分散？可能，但仍然要考虑每个节点意识到自己 ”可以成为 candidate“ 时机不同，网络的快慢，结点的分布都可能成为优势。</p><blockquote><p><em>注：实际编码中用了 rand 函数，让每个节点每次选举时间是不同的</em>  </p></blockquote><h2 id="mit6-824-raft实验部分"><a href="#mit6-824-raft实验部分" class="headerlink" title="mit6.824 raft实验部分"></a>mit6.824 raft实验部分</h2><h3 id="Lab2B"><a href="#Lab2B" class="headerlink" title="Lab2B"></a>Lab2B</h3><h4 id="A0：日志复制怎么保证一致性特性？"><a href="#A0：日志复制怎么保证一致性特性？" class="headerlink" title="A0：日志复制怎么保证一致性特性？"></a>A0：日志复制怎么保证一致性特性？</h4><p>流程：</p><ul><li><strong>Leader 将日志条目追加到本地日志</strong>，并通过 <code>nextIndex[]</code> 向各 Follower 发送。</li><li><strong>Follower 接收到日志条目</strong>，并通过 <code>matchIndex[]</code> 反馈成功复制的最大日志索引。</li><li><strong>Leader 通过 <code>matchIndex[]</code> 判断日志条目是否被大多数节点复制</strong>，如果满足法定人数，则更新 <code>commitIndex</code>，并通知所有节点提交该条目。<em>（peer 在下一次的心跳或者 append entry 中 leaderCommit 检查是否可以更新）</em></li><li><strong>Follower 更新 <code>commitIndex</code> 和 <code>appliedIndex</code></strong>，将已提交的日志条目应用到本地状态机。<blockquote><p>[!quote] Raft 算法里面的一些结构体说明。</p><ul><li>commitIndex ：表示当前已被<strong>提交</strong>的日志条目的索引，在 follower leader 中均有。更新是在 Leader 将日志条目复制到大多数节点之后，由 Leader 来更新。</li><li>appliedIndex ：表示前已应用到本地状态机的日志条目的最大索引。leader 确定了 commit index 之后，确认超过半数就可以写入到本地状态机了，即更新。大多数情况是两者相等，如果 appliedIndex &lt; commitIndex ，那么节点会继续应用直至相等。</li><li><em><strong>日志号和任期号同时配合才能确定一个日志</strong></em>， 避免 leader 没处理完挂掉之后，部分副本上有日志，而新选举的 leader 没有，但是 term 更大更权威，所以可以删掉那些过期的日志，毕竟也没有提交写入</li></ul></blockquote></li></ul><h4 id="A1：为什么需要-rf-replicatorCond"><a href="#A1：为什么需要-rf-replicatorCond" class="headerlink" title="A1：为什么需要 rf.replicatorCond ?"></a>A1：为什么需要 <code>rf.replicatorCond</code> ?</h4><p>这是优化的部分，比较难懂。首先这是每一个 server 维护了条件变量的数组（每一个 server 对应一个条件变量 replicatorCond 数组，而不是一个条件变量），当 server 被 make 函数启动时，replicatorCond 是一直在循环中 wait 的，直到他成为 leader 并且检查到某一个 follower 没有跟上自己的最新日志，那么就会唤醒去执行 replicaOneround。</p><p>那么这个条件变量由谁来唤醒呢？是上层应用调用 <code>start</code>，进一步 <code>broadcastHearbeat()</code> 提醒：成为 leader 之后，复制日志的时候要给每一个 peer 提醒，<code>replicatorCond[peer].Siganl()</code> 函数派上用场。如何优化的，即使被唤醒后，可能某个 peer 的 <code>commitindex</code> 再重复发送日志的过程中并没有变化，即不满足<code> rf.commitindex[peer] &lt; rf.getlastlog().index</code> 那么就没必要给这个 peer 也发送，节省了 RPC 通信</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(rf *Raft)</span></span> applier() &#123;<br><span class="hljs-keyword">for</span> !rf.killed() &#123;<br>rf.mu.Lock()<br><span class="hljs-keyword">for</span> rf.lastApplied &gt;= rf.commitIndex &#123;<br>rf.applyCond.Wait()<br>&#125;<br><br>commitIndex, appliedIndex, firstIndex := rf.commitIndex, rf.lastApplied, rf.getFirstLog().Index<br>entries := <span class="hljs-built_in">make</span>([]Entry, commitIndex-appliedIndex)<br><span class="hljs-built_in">copy</span>(entries, rf.logs[appliedIndex+<span class="hljs-number">1</span>-firstIndex:commitIndex+<span class="hljs-number">1</span>-firstIndex])<br>rf.mu.Unlock()<br><br><span class="hljs-keyword">for</span> *, entry := <span class="hljs-keyword">range</span> entries &#123;<br>rf.applyCh &lt;- ApplyMsg&#123;<br>CommandValid: <span class="hljs-literal">true</span>,<br>Command:      entry.Command,<br>CommandIndex: entry.Index,<br>CommandTerm:  entry.Term,<br>&#125;<br>&#125;<br><br>rf.mu.Lock()<br>DPrintf(<span class="hljs-string">&quot;leader%v send ApplyMsg to channel&quot;</span>, rf.me)<br>rf.lastApplied = Max(rf.lastApplied, commitIndex)<br>rf.mu.Unlock()<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="A2：日志复制的工作机制"><a href="#A2：日志复制的工作机制" class="headerlink" title="A2：日志复制的工作机制"></a>A2：日志复制的工作机制</h4><p>上层应用调用 start() 对 leader 做了以下修改，后续的复制由此事件驱动：</p><ul><li><code>append(rf.logs, newEntry)</code> 修改 logs 数组，从而 getlastlogIndex 也变化</li><li><code>rf.commitIndex, rf.nextIndex[rf.me] = newEntry.Index, newEntry.Index+1</code><br>整个过程的核心是 replicateOneRound 函数，它封装了三个核心操作：<strong>生成请求 request–generateAppendEntryRequest；RPC 调用 follower 的 AppendEntries ；处理返回响应。</strong> 上层调用 appendEntry 和 leader 。心跳都会生成请求，这里生成 request 都用了一个函数统一，核心在于生成 entry 的时候 <code>copy(entries, rf.logs[nextlogIndex-firstIndex:])</code> 。如果是心跳包，那么 peer 节点的 nextlogIndex &#x3D;&#x3D; leader.lastlog.index 所以 entry 空；如果是 append entry 那么 peer 节点的 nextlog.index 就会 &lt; leader.lastlog.index 从而把每一个节点需要的日志都精准地复制下去。这样就实现了日志复制和心跳的统一。</li></ul><p>接下来关注一下复制过程当中非常核心的几个状态：<em><strong>更新 leader&#x2F;peer 的 commitIndex、提交到状态机、日志冲突回溯</strong></em>。</p><ul><li>leader 当客户端上层调用传递了一个指令，leader 就会把这个指令包装成一个日志添加到自己的日志数组当中，<strong>立即更新 commitIndex</strong>。这里可以理解成 leader 立刻收到了来自自己的日志复制请求并接受。</li><li>peer 节点接收到 leader 的日志复制请求之后，检查合法性添加到日志数组，但这个时候并不会更新自己的 commit index，他会检查请求当中的 leader commit 如果 &gt; 自己的 commit index 那么就会更新。所以可以说普通节点 commit index 的更新是要延迟于 leader 的。</li><li>apply 状态： leader 节点在处理 peer 节点返回的响应<strong>如果发现大多数节点都已经成功的复制了日志，并且自己经过了检查——即将应用的日志的任期 &#x3D;&#x3D; 自己当前的任期</strong>（<em>背后有场景</em>），那么就会提交到客户端上层应用。如果不满足条件就会保持 commitIndex 不动。成功唤醒应用线程，应用到状态机之后更新自己的 lastapplied。</li><li>普通节点同时还会另外开一个线程不断去检测自己的 commit index 和 last applied 两个指标如果一旦发现 commit index &gt; last applied 那么就会更新并且提交于自己的状态机中（使用到了信号量，上面已详述）</li><li>日志冲突回溯：这个问题是为了解决<strong>leader 宕机导致的日志仅复制到了小部分节点上新的 leader 为了覆盖这部分旧的日志数据</strong>， 就会在 peer 节点的 prevlogIndex prevlog.term 检查，直到找到 <code>request.prevlog.term == rf.log[index].term</code>。这个问题背后出现的场景很复杂，也是日志复制过程中的边缘场景。</li></ul><h4 id="A3：Raft-的安全性保证"><a href="#A3：Raft-的安全性保证" class="headerlink" title="A3：Raft 的安全性保证"></a>A3：Raft 的安全性保证</h4><p>上面的介绍当中需要补充两点关于 Raft 对安全性保障的说明，首先 leader 节点在把日志应用到状态机的检查中，需要保证应用的 log.term &#x3D;&#x3D; leader.current.term。为什么要做这样的约束呢？ leader 节点再检查到某个日志被大多数节点所复制为什么不能够直接向上层应用提交状态呢?<br><img src="/img/gfs/436500e461cad813e3b489d729e0c6ad.png"><br>目的是为了 <em><strong>防止出现目前最新任期的日志主备切换，导致这种 log index 相同而任期不同的日志覆盖的情况</strong></em> 。上图中如果 S1 S2 S3 都应用日志二，那么当时的 leader 就会像上层调用反应日志二已经被应用到了大多数节点的机器上。但是如果 S5 此时当选成了 leader，就会凭借<strong>索引相同但任期号更大的日志 3</strong>覆盖掉原来日志 2 节点，这样就出现了客户端和实际机器集群当中的不一致状态。所以，Raft 要求 leader 节点要 <em><strong>新增一个自己的 append entry 才能够保护自己之前的日志能够提交。</strong></em></p><p>第二个安全性问题是：为什么 leader 单点向客户端提交状态是可行的。换言之，为什么 leader 向客户端返回的应用日志索引之前的所有日志，都已经确保可以提交了？<br>这是由 <strong>request vote.lastlog.Index&#x2F;term</strong> 保证，每一个 candidate 都会<em><strong>向其他所有的节点发送自己的最后日志的索引和任期号</strong></em>。这就要求了，如果他能够当选 leader，那么大多数的节点都认可他的最新日志。这也就避免了某一些 candidate 不符合大多数节点的日志状态，但是却当选 leader 的情况。也是 raft 中过半哲学能够维持的一致性体现</p>]]></content>
    
    
    <categories>
      
      <category>论文笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>分布式算法与实现</tag>
      
      <tag>raft</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>VM-ft</title>
    <link href="/vm-ft/"/>
    <url>/vm-ft/</url>
    
    <content type="html"><![CDATA[<p>首先可以看<a href="https://tanxinyu.work/vm-ft-thesis/">tanxinyu 的论文笔记博客</a> 对整个论文有大致的了解。Vmware-ft 论文主要为了解决两台主备物理机之间针对非确定事件的一致性同步问题，并对机器状态接受做了问题简化，只关注外部不确定事件输入（这里特别指网络数据包的到达）、定时器中断和怪异指令。他的底层思想仍然是复制状态机模型：<strong>多个机器在相同的初始状态下，执行相同的输入，那么最后的状态一定是相同的</strong>。需要解决的问题是，这些非确定的事件怎么依赖一种 <strong>捕获——重放机制</strong>以确定的顺序和位置记录下来，并精确的在备机上重放，在多台机器上达成同步的一致性状态。可以说，vm-ft 是补充实际应用下，除确定性指令外实现复制状态机的解决方案。</p><h3 id="A1：捕获—模拟中断—重放机制的工作原理"><a href="#A1：捕获—模拟中断—重放机制的工作原理" class="headerlink" title="A1：捕获—模拟中断—重放机制的工作原理"></a>A1：捕获—模拟中断—重放机制的工作原理</h3><p>这里有两个场景，分别是**<em>primary 的定时器中断和来自网卡的网络包数据中断</em>**，处理略有不同：</p><ul><li>定时器中断：在运行了 Primary 虚机的物理服务器上，有一个定时器，这个定时器会计时，生成定时器中断并发送给 VMM（虚拟机监控）。VMM 会<strong>停止 Primary 虚机的指令执行，并记下当前的指令序号</strong>，然后<strong>在指令序号的位置插入伪造的模拟定时器中断</strong>，并恢复 Primary 虚机的运行。之后，VMM 将指令序号和定时器中断再发送给 Backup 虚机。虽然 Backup 虚机的 VMM 也可以从自己的物理定时器接收中断，但是它并没有将这些物理定时器中断传递给 Backup 虚机的 guest 操作系统，而是直接忽略它们。<strong>backup 接收到来自 primary 的伪造 log 中断，送入操作系统中</strong>。这样，就实现了从 primary 到 backup 的精确指令中断重放。</li><li>网卡中断：一个网络数据包到达了 primary 机器，网卡会将网络数据包拷贝给 VMM 的内存， VMM 在接收到网卡中断后会停止 primary 机器。这时候他会同时 将 primary 机器上的指令、网络数据包发送给 backup， backup 拷贝数据包内容到内存，在相同的指令序号位置模拟一个网卡中断；网络数据包拷贝给 primary 内存，发送网卡中断。这就是 Bounce-Buffer 机制<br>可以看到，无论是定时器中断还是网络数据包，其本身的中断都是不确定的，vm-ft 提出用这种指令序列 + 捕获中断 + 重放的方式精准在主备机器上实现同步。</li></ul><h3 id="A2：考虑容错机制，vm-ft-怎么解决宕机的不一致？"><a href="#A2：考虑容错机制，vm-ft-怎么解决宕机的不一致？" class="headerlink" title="A2：考虑容错机制，vm-ft 怎么解决宕机的不一致？"></a>A2：考虑容错机制，vm-ft 怎么解决宕机的不一致？</h3><p>问题背景：假设 Primary 生成了回复给客户端，但是之后立马崩溃了，或者因为网络不稳定，backup 没有收到网络包。primary 机向客户端回复后，backup 成为主机却没有上次的记录。<br>解决这个问题方法可以是等待 ack，即<strong>直到 Backup 虚机确认收到了相应的 Log 条目，Primary 虚机不允许生成任何输出</strong>。这里的核心思想是，确保在客户端看到对于请求的响应时，Backup 虚机一定也看到了对应的请求，或者说至少在 Backup 的 VMM 中缓存了这个请求。如果出现崩溃，那么主机不会收到 ack，自然也不会向客户端返回响应。<br>然而，这样是非常消耗性能的。更通常的做法是，备机维护一个 log 缓冲区，收集主机的网络包并依次确认，而这似乎并没有遵循 vm-ft 的输出控制</p><h3 id="A3：vm-ft-主从切换仍然需要单点控制"><a href="#A3：vm-ft-主从切换仍然需要单点控制" class="headerlink" title="A3：vm-ft 主从切换仍然需要单点控制"></a>A3：vm-ft 主从切换仍然需要单点控制</h3><p>如果两个机器之间发生了网络故障，或者本身两台机器出现物理故障，在其中一个的视角，是：网络数据包不可达或者超时。这时候需要主从切换，向一个可信的第三方 test-and-set 求证。类似锁的功能，决定谁是主节点，用标志位 0&#x2F;1 来标识</p>]]></content>
    
    
    <categories>
      
      <category>论文笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>分布式算法与实现</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>GFS课堂笔记</title>
    <link href="/GFS/"/>
    <url>/GFS/</url>
    
    <content type="html"><![CDATA[<h2 id="GFS"><a href="#GFS" class="headerlink" title="GFS"></a>GFS</h2><p>分布式目标是为了提升性能，同时机器过多可能会带来大量不定时出现的故障，因此需要有容错和监控系统。解决故障的方法是创建副本，那么又带来多个副本之间可能出现的不一致问题。</p><p>GFS 设计需要解决一些问题</p><h2 id="文件怎么分散存储在多个服务器当中？"><a href="#文件怎么分散存储在多个服务器当中？" class="headerlink" title="文件怎么分散存储在多个服务器当中？"></a>文件怎么分散存储在多个服务器当中？</h2><p>分割成 64MB 大小的 chunk 存储在不同机器下面，一个文件内容可能需要占用多个 chunk（gfs 设计准备 GB 级大小文件）。这个数字偏大，是为了减少 master 节点存储 chunk 信息，查询时尽量在少的 chunk 完成减少网络传输代价。</p><p>✅GFS 保证单点 master，原因是<strong>控制流和数据流分离</strong>，client 仅从 master 获取 chunk 的元数据信息，并不直接进行数据交互，这一点放在 chunkserver 与 client 中。另外，master 只持久化 file and chunk namespace、file 和 chunk id 的映射关系，至于 chunk 在哪个 server 当中并没有保存，因为可以和 chunkserver 通信得到一个 chunkserver 的所有 chunk 信息。</p><h2 id="❓-服务器发生故障怎么保证文件不损坏不丢失？"><a href="#❓-服务器发生故障怎么保证文件不损坏不丢失？" class="headerlink" title="❓ 服务器发生故障怎么保证文件不损坏不丢失？"></a>❓ 服务器发生故障怎么保证文件不损坏不丢失？</h2><p>（<strong>高可用问题</strong>，在宕机之后仍能保证向外提供服务）<br><strong><em>master 的高可用</em></strong>：master 和 元数据的高可用是一致的，采用主备复制的模式。master 更新&#x2F;增加 namespace 或 改变映射关系首先写入日志，再将 wal 从 priamry master 发送到备用 master，直到备用 master 确认之后再更改主 master 的内存。而要识别 master 宕机需要共识算法。<br><strong><em>chunk server 的高可用</em></strong>：通过 master 维持副本数量和副本之间的一致，并不遵循物理上 chunk server 主备一致性。GFS 设定只有三个 chunk 副本都保存了信息才算是写入完成，如果出现某个 chunkserver 宕机，还剩下两个副本可用。同时 master 可以在其他的 chunkserver 上新建副本，维持副本数目。</p><blockquote><p>另外，master 需要将 chunk server 版本号保存起来，因为一旦 master 宕机，那么仅根据 chunk server 自己的版本号并不能确定哪个 server 是保存最新副本的安全 server</p></blockquote><div class="note note-info">                        <div class="note-content">              <p>“当 Master 重启时，无论如何都需要与所有的 Chunk 服务器进行通信，因为 Master 需要确定哪个 Chunk 服务器存了哪个 Chunk。你可能会想到，Master 可以将所有 Chunk 服务器上的 Chunk 版本号汇总，找出里面的最大值作为最新的版本号。如果所有持有 Chunk 的服务器都响应了，那么这种方法是没有问题的。但是存在一种风险，当 Master 节点重启时，可能部分 Chunk 服务器离线或者失联或者自己也在重启，从而不能响应 Master 节点的请求。所以，Master 节点可能只能获取到持有旧副本的 Chunk 服务器的响应，而持有最新副本的 Chunk 服务器还没有完成重启，或者还是离线状态（这个时候 Master 能找到的 Chunk 最大版本明显不对）</p><p>因为 Master 从磁盘存储的数据知道 Chunk 对应的最新版本，Master 节点会整合具有最新版本 Chunk 的服务器。每个 Chunk 服务器会记住本地存储 Chunk 对应的版本号，当 Chunk 服务器向 Master 汇报时，就可以说，我有这个 Chunk 的这个版本。而 Master 节点就可以忽略哪些版本号与已知版本不匹配的 Chunk 服务器。<br>”</p>            </div>          </div><h2 id="❓primary-chunk-有什么用？"><a href="#❓primary-chunk-有什么用？" class="headerlink" title="❓primary chunk 有什么用？"></a>❓primary chunk 有什么用？</h2><p>控制流的顺序由其决定，租约期限内决定其他副本写入 chunk 顺序，避免同时副本写入的顺序不对。依然还是控制流的主备关系，client 还是正常发送所有的数据给副本，只是主备关系决定了写入的顺序。</p><h2 id="🤔GFS-读写流程"><a href="#🤔GFS-读写流程" class="headerlink" title="🤔GFS 读写流程"></a>🤔GFS 读写流程</h2><p><img src="/img/gfs/ceec5785d9432faf3f9ebf3c565a1838.png"><br>流水线式数据传输可以节省大量的网络传输，不用再从客户端指定传输到主 chunkserver，那样的话主 chunkserver 需要给多个 replica 发送数据，网络带宽将成为性能瓶颈。<strong>控制流分离，client 仍然需要向主 chunkserver 发送写入顺序信息</strong>，等待所有副本接收到数据后，主 chunk server 决定顺序写入。<br>推荐使用追加的方式写入文件，也是 GFS 在文件写入可以接受 “重复写入数据，但是数据不能错误” 的想法。</p><h2 id="🤔-那么一致性模型的要点是什么呢？"><a href="#🤔-那么一致性模型的要点是什么呢？" class="headerlink" title="🤔 那么一致性模型的要点是什么呢？"></a>🤔 那么一致性模型的要点是什么呢？</h2><p><img src="/img/gfs/51e6890f9b4daaf2696a633b7a0077e9.png"></p><div class="note note-info">                        <div class="note-content">              <p>如果是全局的改写，一次修改可能涉及到多个 chunk，GFS 并没有全局 chunk 的概念，只能保证相同 chunk 副本之间的写入是一致的（这是由数据流和控制流分离保证的，控制流决定了写入的顺序，在上面已经有介绍）；而在多个改写的 chunk 之间，并发的写操作对不同的 chunk 来自 S1 S2 S3 可能不同。<strong><em>不同 chunk 对改写的顺序不能保证相同</em></strong></p>            </div>          </div><div class="note note-info">                        <div class="note-content">              <p><img src="/img/gfs/ff47e22fe31d77dec98cc0a6f62fda87.png"><br>优点很明显：1. 减少内存占用空间，大部分 chunk 并不会修改，快照和原文件的 chunk 是公用的；2. 快速的快照机制，停止写入的时间很短暂，因为只需要将源数据拷贝给快照文件，增加引用计数即可</p>            </div>          </div><h2 id="GFS-一些课堂问题"><a href="#GFS-一些课堂问题" class="headerlink" title="GFS 一些课堂问题"></a>GFS 一些课堂问题</h2><div class="note note-info">                        <div class="note-content">              <p><strong>可不可以通过版本号来判断副本是否有之前追加的数据？</strong>  </p><p>Robert 教授：所有的 Secondary 都有相同的版本号。版本号只会在 Master 指定一个新 Primary 时才会改变。通常只有在原 Primary 发生故障了，才会指定一个新的 Primary。所以，副本（参与写操作的 Primary 和 Secondary）都有相同的版本号，<strong>你没法通过版本号来判断它们是否一样，或许它们就是不一样的（取决于数据追加成功与否）</strong>。</p><p>这么做的理由是，当 Primary 回复“no”给客户端时，客户端知道写入失败了，之后客户端的 GFS 库会重新发起追加数据的请求，直到最后成功追加数据。成功了之后，追加的数据会在所有的副本中相同位置存在。在那之前，追加的数据只会在部分副本中存在。（注：挂掉的副本没有接收到数据，master 也可以通过 checksum 来判断）<br>总结：<strong>版本号不是判断写入是否成功的工具，版本号是用来区分是否错过了换主，通过检查内容&#x2F;校验等方式以及比较版本标记哪些 chunk 是陈旧的。</strong> 😁 客户端重试，并且期望之后能正常工作。并不会立即解决错误的问题<br>如果 Secondary 服务器挂了，Master 节点可以发现并更新 Primary 和 Secondary 的集合，之后再增加版本号。但是这些都是之后才会发生</p>            </div>          </div><div class="note note-info">                        <div class="note-content">              <p><strong>如果 master 发现 primary 挂了怎么办？</strong>  </p><p>Robert 教授：可以这么回答这个问题。在某个时间点，Master 指定了一个 Primary，之后 Master 会一直通过定期的 ping 来检查它是否还存活。因为如果它挂了，Master 需要选择一个新的 Primary。Master 发送了一些 ping 给 Primary，并且 Primary 没有回应，你可能会认为 Master 会在那个时间立刻指定一个新的 Primary。但事实是，这是一个错误的想法。为什么是一个错误的想法呢？因为可能是网络的原因导致 ping 没有成功，所以有可能 Primary 还活着，但是网络的原因导致 ping 失败了。❗ 但同时，Primary 还可以与客户端交互，如果 Master 为 Chunk 指定了一个新的 Primary，那么就会同时有两个 Primary 处理写请求，这两个 Primary 不知道彼此的存在，会分别处理不同的写请求，最终会导致有两个不同的数据拷贝。这被称为脑裂（split-brain）。</p><p>脑裂是一种非常重要的概念，我们会在之后的课程中再次介绍它（详见 6.1），它通常是由网络分区引起的。比如说，Master 无法与 Primary 通信，但是 Primary 又可以与客户端通信，这就是一种网络分区问题。网络故障是这类分布式存储系统中最难处理的问题之一。</p><p>所以，我们想要避免错误的为同一个 Chunk 指定两个 Primary 的可能性。Master 采取的方式是，<strong><em>当指定一个 Primary 时，为它分配一个租约，Primary 只在租约内有效。Master 和 Primary 都会知道并记住租约有多长，当租约过期了，Primary 会停止响应客户端请求，它会忽略或者拒绝客户端请求。</em></strong> 因此，如果 Master 不能与 Primary 通信，并且想要指定一个新的 Primary 时，Master 会等到前一个 Primary 的租约到期。这意味着，Master 什么也不会做，只是等待租约到期。租约到期之后，可以确保旧的 Primary 停止了它的角色，这时 Master 可以安全的指定一个新的 Primary 而不用担心出现这种可怕的脑裂的情况。</p>            </div>          </div><div class="note note-info">                        <div class="note-content">              <p><strong>为什么立即指定一个新的 Primary 是坏的设计？</strong>  </p><p>因为客户端会通过缓存提高效率，客户端会在短时间缓存 Primary 的身份信息（这样，客户端就不用每次都会向 Master 请求 Primary 信息）。即使没有缓存，也可能出现这种情况，客户端向 Master 节点查询 Primary 信息，Master 会将 Primary 信息返回，这条消息在网络中传播。之后 Master 如果发现 Primary 出现故障，并且立刻指定一个新的 Primary，同时向新的 Primary 发消息说，你是 Primary。Master 节点之后会向其他查询 Primary 的客户端返回这个新的 Primary。<strong>而前一个 Primary 的查询还在传递过程中，前一个客户端收到的还是旧的 Primary 的信息。如果没有其他的更聪明的一些机制，前一个客户端是没办法知道收到的 Primary 已经过时了。</strong> 如果前一个客户端执行写文件，那么就会与后来的客户端产生两个冲突的副本。</p>            </div>          </div>]]></content>
    
    
    <categories>
      
      <category>论文笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>分布式算法与实现</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>收集优秀的创作者</title>
    <link href="/source-collection/"/>
    <url>/source-collection/</url>
    
    <content type="html"><![CDATA[<h1 id="解读系列：心理学、成长、历史、政治"><a href="#解读系列：心理学、成长、历史、政治" class="headerlink" title="解读系列：心理学、成长、历史、政治"></a>解读系列：心理学、成长、历史、政治</h1><p><a href="https://space.bilibili.com/302299393">张志浩在剥柚</a>：“不做影视解说，只做过度解读”<br><a href="https://space.bilibili.com/262513?spm_id_from=333.337.0.0">学长表示理解</a>：辩手和心理咨询师的身份让 up 在社会现象和个体心理方面关注细微，是我很喜欢的一位博主。</p><h1 id="论文分析系列：分布式、机器学习……"><a href="#论文分析系列：分布式、机器学习……" class="headerlink" title="论文分析系列：分布式、机器学习……"></a>论文分析系列：分布式、机器学习……</h1><p><a href="https://space.bilibili.com/61981458?spm_id_from=333.337.0.0">戌米的论文笔记</a>：论文讲解特别细致，表述清晰<br><a href="https://space.bilibili.com/1567748478?spm_id_from=333.337.0.0">跟李沐学AI</a>：AI<br><a href="https://tanxinyu.work/">tanxinyu’s blog</a>：数据库、分布式系统</p><h1 id="技术系列："><a href="#技术系列：" class="headerlink" title="技术系列："></a>技术系列：</h1><p><a href="https://icyfenix.cn/">凤凰架构</a>：目前读到的最棒的架构书，兼具核心原理和技术细节，条理非常清楚<br><a href="http://nil.csail.mit.edu/6.824/2021/index.html">MIT 6.824</a>：网红课，分布式系统的课程材料和实验对本人的兴趣启蒙帮助很大<br><a href="https://space.bilibili.com/689233562?spm_id_from=333.337.search-card.all.click">厦门大学的JavaEE课程</a><br><a href="https://space.bilibili.com/1324259795/lists">硬核课堂</a>：AI-infra、系统设计、源码类. <a href="https://hardcore.feishu.cn/wiki/wikcn9i4sfdTkxVX0DYrMMbNBpd">这里</a>是飞书文档集合<br><a href="https://bytebytego.com/">ByteByteGo</a>：科普技术风格的教程，图示很清晰易读<br><a href="https://tech.codelc.com/">lcomplete技术分享</a>：周刊和一些心得分享<br><a href="https://www.pdai.tech/">Java全栈技术</a>：从Java基础、算法到框架、架构技术栈<br><a href="https://freegeektime.com/posts/100006701/">极客时间：Java核心技术精讲</a><br><a href="https://pphc.lvwenhan.com/">PPHC 高并发的哲学原理</a></p><h2 id="技术博主"><a href="#技术博主" class="headerlink" title="技术博主"></a>技术博主</h2><p><a href="https://space.bilibili.com/7968519?spm_id_from=333.337.0.0">学Java的生生</a>：Java源码分析，手写系列，设计模式系列……<br><a href="https://space.bilibili.com/3493077649983936?spm_id_from=333.1387.follow.user_card.click">_王帅真</a>：技术分享，后端的专栏很用心。<a href="https://blog.qizong007.top/">这里</a>是他的博客网站，做了很多总结和有深度的思考<br><a href="https://space.bilibili.com/392470114?spm_id_from=333.1387.follow.user_card.click">架构驿站</a>：spring、消息队列、Mysql 进阶课……<br>面经 &amp; 八股文  </p><ul><li>Mysql: <a href="https://freegeektime.com/posts/100020801/">实战45讲</a>、</li><li>Java: <a href="https://freegeektime.com/posts/100039001/">设计之美</a>、<a href="https://github.com/TangBean/understanding-the-jvm">JVM笔记</a></li><li>架构: <a href="https://learning-guide.gitbook.io/system-design-interview">Alex xu系统设计面试</a></li></ul><h1 id="偏好"><a href="#偏好" class="headerlink" title="偏好"></a>偏好</h1><p><a href="https://space.bilibili.com/1384025112?spm_id_from=333.337.0.0">华农狮山长跑队</a><br><a href="https://space.bilibili.com/602553583?spm_id_from=333.337.0.0">贾一凡_F</a><br><a href="https://space.bilibili.com/1575688490?spm_id_from=333.337.0.0">CLS同学</a><br><a href="https://space.bilibili.com/7226269?spm_id_from=333.337.0.0">麦片是只小金毛</a></p><h1 id="TODO"><a href="#TODO" class="headerlink" title="TODO"></a>TODO</h1><p><a href="">代码大全</a><br><a href="https://datawhalechina.github.io/happy-llm/#/">DataWhale: Happy-LLM</a><br><a href="https://space.bilibili.com/1369507485/lists/615109?type=season">推荐系统公开课</a></p>]]></content>
    
    
    <categories>
      
      <category>WLB</category>
      
    </categories>
    
    
    <tags>
      
      <tag>收集</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
